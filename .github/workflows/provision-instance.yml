name: Provision LocalNodes Instance

on:
  workflow_dispatch:
    inputs:
      subdomain:
        description: 'Subdomain (e.g., "portland" → portland.localnodes.xyz)'
        required: true
        type: string
      site_name:
        description: 'Site display name (e.g., "Portland LocalNodes")'
        required: true
        type: string
      demo_module:
        description: 'Demo content module (none = blank instance)'
        required: true
        type: choice
        options:
          - localnodes_demo
          - boulder_demo
          - portland_demo
          - none
        default: localnodes_demo
      email:
        description: 'Organizer email address'
        required: false
        type: string
      stripe_session_id:
        description: 'Stripe Checkout Session ID for state tracking'
        required: false
        type: string
      callback_url:
        description: 'Base URL to POST status updates to (e.g., https://localnodes.xyz)'
        required: false
        type: string

env:
  COOLIFY_API: https://coolify.localnodes.xyz/api/v1
  COOLIFY_PROJECT: n0sc0wcog8k4kkc48k0k0wo0
  COOLIFY_SERVER: q4okokcg8occ88w00c4kg0sw
  COOLIFY_ENV: production
  GIT_REPO: https://github.com/LocalNodes/os-knowledge-garden
  GIT_BRANCH: main

jobs:
  provision:
    runs-on: ubuntu-latest
    steps:
      - name: Validate subdomain
        run: |
          SUBDOMAIN="${{ inputs.subdomain }}"
          # Lowercase, alphanumeric + hyphens only, no leading/trailing hyphen
          if ! echo "$SUBDOMAIN" | grep -qE '^[a-z0-9]([a-z0-9-]*[a-z0-9])?$'; then
            echo "::error::Invalid subdomain '$SUBDOMAIN'. Use lowercase letters, numbers, and hyphens only."
            exit 1
          fi
          # Reserved subdomains
          for reserved in www api coolify mail smtp; do
            if [ "$SUBDOMAIN" = "$reserved" ]; then
              echo "::error::Subdomain '$SUBDOMAIN' is reserved."
              exit 1
            fi
          done
          echo "SUBDOMAIN=$SUBDOMAIN" >> "$GITHUB_ENV"
          echo "FQDN=${SUBDOMAIN}.localnodes.xyz" >> "$GITHUB_ENV"

      - name: Check subdomain availability
        run: |
          # Query Coolify for existing apps with this domain
          APPS=$(curl -s "$COOLIFY_API/applications" \
            -H "Authorization: Bearer ${{ secrets.COOLIFY_API_TOKEN }}" \
            -H "Accept: application/json")

          # Applications list is a plain array (no .data wrapper)
          if echo "$APPS" | jq -e ".[] | select(.docker_compose_domains != null) | .docker_compose_domains" 2>/dev/null | grep -q "$FQDN"; then
            echo "::error::Subdomain '$SUBDOMAIN' is already in use."
            exit 1
          fi
          echo "Subdomain $FQDN is available"

      - name: Create Coolify application
        id: create
        run: |
          # Include docker_compose_domains at creation time so Coolify
          # parses the compose file and sets up domain routing in one step.
          RESPONSE=$(curl -s -X POST "$COOLIFY_API/applications/public" \
            -H "Authorization: Bearer ${{ secrets.COOLIFY_API_TOKEN }}" \
            -H "Content-Type: application/json" \
            -H "Accept: application/json" \
            -d "{
              \"name\": \"$SUBDOMAIN\",
              \"project_uuid\": \"$COOLIFY_PROJECT\",
              \"environment_name\": \"$COOLIFY_ENV\",
              \"server_uuid\": \"$COOLIFY_SERVER\",
              \"git_repository\": \"$GIT_REPO\",
              \"git_branch\": \"$GIT_BRANCH\",
              \"build_pack\": \"dockercompose\",
              \"ports_exposes\": \"80\",
              \"docker_compose_location\": \"docker-compose.yaml\",
              \"docker_compose_domains\": [{\"name\": \"opensocial\", \"domain\": \"https://$FQDN\"}]
            }")

          APP_UUID=$(echo "$RESPONSE" | jq -r '.uuid')
          if [ -z "$APP_UUID" ] || [ "$APP_UUID" = "null" ]; then
            echo "::error::Failed to create app: $RESPONSE"
            exit 1
          fi

          echo "APP_UUID=$APP_UUID" >> "$GITHUB_ENV"
          echo "Created application $APP_UUID"

      - name: Configure environment variables
        run: |
          # Wait for Coolify to fully parse compose file and generate env vars.
          # Coolify creates one env var per ${VAR:-default} reference in the compose
          # file. This needs time to complete before we can deduplicate and update.
          echo "Waiting for Coolify to parse compose file..."
          sleep 15

          update_env() {
            local KEY="$1" VALUE="$2"
            local HTTP_CODE
            HTTP_CODE=$(curl -s -o /dev/null -w '%{http_code}' -X PATCH "$COOLIFY_API/applications/$APP_UUID/envs" \
              -H "Authorization: Bearer ${{ secrets.COOLIFY_API_TOKEN }}" \
              -H "Content-Type: application/json" \
              -d "{\"key\": \"$KEY\", \"value\": \"$VALUE\"}")
            if [ "$HTTP_CODE" = "201" ] || [ "$HTTP_CODE" = "200" ]; then
              echo "  Updated $KEY"
            else
              echo "  Warning: $KEY update returned HTTP $HTTP_CODE"
            fi
          }

          # Delete duplicate env vars (Coolify creates one per compose service
          # that references the same ${VAR}, resulting in duplicates)
          dedup_envs() {
            local ENV_VARS
            ENV_VARS=$(curl -s "$COOLIFY_API/applications/$APP_UUID/envs" \
              -H "Authorization: Bearer ${{ secrets.COOLIFY_API_TOKEN }}" \
              -H "Accept: application/json")

            local DUPES
            DUPES=$(echo "$ENV_VARS" | jq -r '[.[].key] | group_by(.) | .[] | select(length > 1) | .[0]')
            if [ -z "$DUPES" ]; then
              echo "  No duplicates found"
              return
            fi

            for KEY in $DUPES; do
              local UUIDS
              UUIDS=$(echo "$ENV_VARS" | jq -r "[.[] | select(.key == \"$KEY\")][1:][].uuid")
              for UUID in $UUIDS; do
                curl -s -X DELETE "$COOLIFY_API/applications/$APP_UUID/envs/$UUID" \
                  -H "Authorization: Bearer ${{ secrets.COOLIFY_API_TOKEN }}" > /dev/null
                echo "  Deleted duplicate $KEY ($UUID)"
              done
            done
          }

          echo "Cleaning duplicate env vars (pass 1)..."
          dedup_envs

          echo "Setting instance-specific values..."
          update_env "DEMO_MODULE" "${{ inputs.demo_module }}"
          update_env "DRUPAL_SITE_NAME" "${{ inputs.site_name }}"
          update_env "DRUPAL_ADMIN_PASS" "localnodes2026"
          update_env "GEMINI_API_KEY" "${{ secrets.GEMINI_API_KEY }}"

          # Generate unique secrets for this instance
          update_env "DB_PASSWORD" "$(openssl rand -hex 16)"
          update_env "DB_ROOT_PASSWORD" "$(openssl rand -hex 16)"
          update_env "DRUPAL_HASH_SALT" "$(openssl rand -hex 32)"

          # Second dedup pass — catches any stragglers from async compose parsing
          echo "Cleaning duplicate env vars (pass 2)..."
          dedup_envs

          echo "Environment configured"

      - name: Deploy instance
        run: |
          # Deploy endpoint is GET /deploy?uuid=...&force=true (not POST /applications/{uuid}/deploy)
          RESPONSE=$(curl -s "$COOLIFY_API/deploy?uuid=$APP_UUID&force=true" \
            -H "Authorization: Bearer ${{ secrets.COOLIFY_API_TOKEN }}" \
            -H "Accept: application/json")

          # Response is { "deployments": [...] } (no .data wrapper)
          DEPLOY_UUID=$(echo "$RESPONSE" | jq -r '.deployments[0].deployment_uuid // empty')
          if [ -z "$DEPLOY_UUID" ] || [ "$DEPLOY_UUID" = "null" ]; then
            echo "::error::Failed to trigger deployment: $RESPONSE"
            exit 1
          fi

          echo "DEPLOY_UUID=$DEPLOY_UUID" >> "$GITHUB_ENV"
          echo "Deployment triggered: $DEPLOY_UUID"

      - name: Notify status - provisioning
        if: inputs.callback_url != '' && inputs.stripe_session_id != ''
        run: |
          curl -sf -X POST "${{ inputs.callback_url }}/api/provision-callback" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.PROVISION_CALLBACK_SECRET }}" \
            -d "{
              \"session_id\": \"${{ inputs.stripe_session_id }}\",
              \"status\": \"provisioning\",
              \"site_url\": \"https://$FQDN\"
            }" || echo "Warning: callback failed (non-fatal)"

      - name: Wait for deployment
        run: |
          echo "Waiting for deployment to finish (build + container start)..."
          for i in $(seq 1 30); do
            sleep 10
            # Response is flat object with .status at root (no .data wrapper)
            STATUS=$(curl -s "$COOLIFY_API/deployments/$DEPLOY_UUID" \
              -H "Authorization: Bearer ${{ secrets.COOLIFY_API_TOKEN }}" \
              -H "Accept: application/json" | jq -r '.status // "unknown"')

            echo "  [$((i * 10))s] Status: $STATUS"
            if [ "$STATUS" = "finished" ]; then
              echo "Deployment complete"
              break
            elif [ "$STATUS" = "failed" ] || [ "$STATUS" = "cancelled" ]; then
              echo "::error::Deployment $STATUS"
              exit 1
            fi
          done

      - name: Notify status - installing
        if: inputs.callback_url != '' && inputs.stripe_session_id != ''
        run: |
          curl -sf -X POST "${{ inputs.callback_url }}/api/provision-callback" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.PROVISION_CALLBACK_SECRET }}" \
            -d "{
              \"session_id\": \"${{ inputs.stripe_session_id }}\",
              \"status\": \"installing\",
              \"site_url\": \"https://$FQDN\"
            }" || echo "Warning: callback failed (non-fatal)"

      - name: Wait for site to become healthy
        run: |
          echo "Waiting for Drupal install + healthcheck (up to 10 min)..."
          for i in $(seq 1 60); do
            sleep 10
            HTTP_CODE=$(curl -so /dev/null -w '%{http_code}' "https://$FQDN/user/login" 2>/dev/null || echo "000")
            echo "  [$((i * 10))s] https://$FQDN -> HTTP $HTTP_CODE"
            if [ "$HTTP_CODE" = "200" ]; then
              echo "Site is live at https://$FQDN"
              break
            fi
          done

          echo "HTTP_CODE=$HTTP_CODE" >> "$GITHUB_ENV"
          if [ "$HTTP_CODE" != "200" ]; then
            echo "::warning::Site not yet responding after 10 min — may still be installing"
          fi

      - name: Setup SSH
        if: env.HTTP_CODE == '200' && inputs.email != ''
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Notify status - creating user
        if: env.HTTP_CODE == '200' && inputs.email != '' && inputs.callback_url != '' && inputs.stripe_session_id != ''
        run: |
          curl -sf -X POST "${{ inputs.callback_url }}/api/provision-callback" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.PROVISION_CALLBACK_SECRET }}" \
            -d "{
              \"session_id\": \"${{ inputs.stripe_session_id }}\",
              \"status\": \"creating_user\",
              \"site_url\": \"https://$FQDN\"
            }" || echo "Warning: callback failed (non-fatal)"

      - name: Create organizer admin account
        if: env.HTTP_CODE == '200' && inputs.email != ''
        run: |
          # Add host key to known_hosts to avoid interactive prompt
          ssh-keyscan -H localnodes.xyz >> ~/.ssh/known_hosts 2>/dev/null

          # Find the opensocial container
          CONTAINER=$(ssh root@localnodes.xyz "docker ps --filter 'name=${{ inputs.subdomain }}' --filter 'name=opensocial' --format '{{.Names}}' | head -1")
          if [ -z "$CONTAINER" ]; then
            echo "::error::Could not find opensocial container for ${{ inputs.subdomain }}"
            exit 1
          fi
          echo "Found container: $CONTAINER"

          DRUSH="docker exec $CONTAINER /var/www/html/vendor/bin/drush -r /var/www/html/html --uri=https://$FQDN"

          # Generate secure random password
          PASSWORD=$(openssl rand -hex 16)

          # Create user with community name + " Admin" as username
          USERNAME="${{ inputs.site_name }} Admin"
          ssh root@localnodes.xyz "$DRUSH user:create \"$USERNAME\" --mail=\"${{ inputs.email }}\" --password=\"$PASSWORD\"" 2>&1 || echo "Warning: user may already exist"

          # Assign sitemanager role
          ssh root@localnodes.xyz "$DRUSH user:role:add sitemanager \"$USERNAME\"" 2>&1 || echo "Warning: role may already be assigned"

          # Generate one-time login URL
          LOGIN_URL=$(ssh root@localnodes.xyz "$DRUSH uli --name=\"$USERNAME\" --uri=https://$FQDN" 2>/dev/null)
          echo "LOGIN_URL=$LOGIN_URL" >> "$GITHUB_ENV"
          echo "Login URL generated for ${{ inputs.email }}"

      - name: Send welcome email
        if: env.LOGIN_URL != '' && inputs.email != ''
        run: |
          # Notify status - sending email
          if [ -n "${{ inputs.callback_url }}" ] && [ -n "${{ inputs.stripe_session_id }}" ]; then
            curl -sf -X POST "${{ inputs.callback_url }}/api/provision-callback" \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${{ secrets.PROVISION_CALLBACK_SECRET }}" \
              -d "{
                \"session_id\": \"${{ inputs.stripe_session_id }}\",
                \"status\": \"sending_email\",
                \"site_url\": \"https://$FQDN\",
                \"login_url\": \"$LOGIN_URL\"
              }" || echo "Warning: callback failed (non-fatal)"
          fi

          # Send welcome email via Resend API
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "https://api.resend.com/emails" \
            -H "Authorization: Bearer ${{ secrets.RESEND_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"from\": \"LocalNodes <hello@localnodes.xyz>\",
              \"to\": [\"${{ inputs.email }}\"],
              \"subject\": \"Your knowledge garden is ready!\",
              \"html\": \"<h1>Welcome to LocalNodes!</h1><p>Your knowledge garden at <a href='https://$FQDN'>https://$FQDN</a> is ready.</p><p><a href='$LOGIN_URL'>Click here to set your password and log in</a></p><p>This login link is single-use and will expire after first use. If it expires, you can request a new one from the login page.</p><p>Happy gardening!<br>The LocalNodes Team</p>\"
            }")

          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | head -n -1)
          if [ "$HTTP_CODE" = "200" ]; then
            echo "Welcome email sent to ${{ inputs.email }}"
          else
            echo "::warning::Welcome email failed (HTTP $HTTP_CODE): $BODY"
          fi

      - name: Notify status - complete
        if: env.HTTP_CODE == '200' && inputs.callback_url != '' && inputs.stripe_session_id != ''
        run: |
          curl -sf -X POST "${{ inputs.callback_url }}/api/provision-callback" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.PROVISION_CALLBACK_SECRET }}" \
            -d "{
              \"session_id\": \"${{ inputs.stripe_session_id }}\",
              \"status\": \"complete\",
              \"site_url\": \"https://$FQDN\",
              \"login_url\": \"${LOGIN_URL:-}\"
            }" || echo "Warning: callback failed (non-fatal)"

      - name: Summary
        run: |
          cat >> "$GITHUB_STEP_SUMMARY" << EOF
          ## LocalNodes Instance Provisioned

          | | |
          |---|---|
          | **URL** | https://$FQDN |
          | **Site Name** | ${{ inputs.site_name }} |
          | **Demo Content** | ${{ inputs.demo_module }} |
          | **Coolify UUID** | \`$APP_UUID\` |
          | **Admin Login** | admin / localnodes2026 |
          | **Organizer Email** | ${{ inputs.email }} |
          | **Login URL** | ${{ env.LOGIN_URL }} |

          ### Next Steps
          - Visit https://$FQDN and verify the site
          - Log in and customize content
          - Manage via [Coolify Dashboard](https://coolify.localnodes.xyz)
          EOF

      - name: Notify status - failed
        if: failure() && inputs.callback_url != '' && inputs.stripe_session_id != ''
        run: |
          curl -sf -X POST "${{ inputs.callback_url }}/api/provision-callback" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.PROVISION_CALLBACK_SECRET }}" \
            -d "{
              \"session_id\": \"${{ inputs.stripe_session_id }}\",
              \"status\": \"failed\",
              \"error\": \"Provisioning workflow failed. Check GitHub Actions logs for details.\"
            }" || echo "Warning: callback failed (non-fatal)"
