---
phase: 10-production-config-management-deploy-flow
verified: 2026-03-02T14:30:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
human_verification:
  - test: "Trigger a redeploy of an existing Coolify instance without wiping volumes"
    expected: "Entrypoint detects existing install, runs drush deploy, config changes apply without data loss"
    why_human: "Cannot simulate container start against live Coolify/MariaDB from local filesystem checks"
  - test: "Run drush status in a Docker container built from current Dockerfile"
    expected: "Bootstrap: Successful, config_sync_directory resolves to /var/www/html/config/sync with 1057 YAML files"
    why_human: "Path resolution (../config/sync relative to Drupal root) is correct by reasoning but requires a running container to confirm"
---

# Phase 10: Production Config Management & Deploy Flow — Verification Report

**Phase Goal:** Config changes deploy incrementally to existing instances via `drush deploy` instead of requiring volume wipes. Settings.php is version-controlled with `getenv()`. Config/sync is complete and shipped in the Docker image.
**Verified:** 2026-03-02T14:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | settings.php is a committed template with getenv() calls, not generated by the entrypoint | VERIFIED | 105-line template at `html/sites/default/settings.php`; 15 `getenv()` calls; no `SETTINGS_EOF` or generation code in entrypoint |
| 2 | Config/sync directory is included in the Docker image for drush deploy | VERIFIED | `docker/Dockerfile` line 115: `COPY config/ /var/www/html/config/`; 1057 YAML files in `config/sync/` |
| 3 | config_exclude_modules excludes demo modules; web3 modules are core platform | VERIFIED | `settings.php` lines 44-49: only demo modules excluded (localnodes_demo, boulder_demo, portland_demo, social_demo); web3 modules removed after Plan 03 fix (commit ddcdacb) |
| 4 | config/sync is complete — localnodes_platform + web3 modules in core.extension, gemini key config present, demo modules removed | VERIFIED | `localnodes_platform: 0` at line 81; `siwe_login: 0`, `safe_smart_accounts: 0`, `group_treasury: 0`, `social_group_treasury: 0` present; `localnodes_demo` and `social_demo` absent; `key.key.gemini_api_key.yml` exists with `key_provider: env` |
| 5 | Entrypoint uses `drush deploy` for existing installs (no more config:import --partial) | VERIFIED | `docker/entrypoint.sh` line 142: `$DRUSH deploy -y` in existing-install path; zero occurrences of `config:import --partial`; zero settings.php generation code |
| 6 | Deploy hook scaffold exists for future one-time operations | VERIFIED | `html/modules/custom/localnodes_platform/localnodes_platform.deploy.php` exists with correct docblock and naming convention |

**Score: 6/6 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `html/sites/default/settings.php` | Template with getenv(), config_sync_directory, config_exclude_modules, reverse proxy, DDEV integration | VERIFIED | 105 lines, 15 getenv() calls, all required sections present |
| `docker/Dockerfile` | COPY config/ /var/www/html/config/ for drush deploy | VERIFIED | Line 115 confirmed |
| `html/modules/custom/localnodes_platform/localnodes_platform.deploy.php` | Deploy hook scaffold with naming convention docs | VERIFIED | Exists, 13 lines, contains docblock with drush.org reference |
| `config/sync/core.extension.yml` | localnodes_platform present; demo/web3 modules absent (demo) or present (web3) | VERIFIED | localnodes_platform at line 81; web3 modules at lines 64, 101, 106, 124; no localnodes_demo or social_demo |
| `config/sync/key.key.gemini_api_key.yml` | Gemini API key entity with env provider | VERIFIED | Exists with `key_provider: env`, `env_variable: GEMINI_API_KEY` |
| `docker/entrypoint.sh` | drush deploy for existing installs, no config:import --partial, no settings.php generation | VERIFIED | Lines 141-142 confirmed; zero old patterns found |
| `.claude/skills/localnodes-coolify-deploy/SKILL.md` | drush deploy workflow documented; config:import --partial removed | VERIFIED (with caveat) | drush deploy present at lines 29, 54, 171, 183, 193; config:import --partial absent; but two stale web3 statements remain (see Anti-Patterns) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `html/sites/default/settings.php` | `config/sync/` | `$settings['config_sync_directory'] = '../config/sync'` | WIRED | Line 34 confirmed; path resolves to `/var/www/html/config/sync` in Docker (Drupal root `/var/www/html/html` + `../config/sync`) |
| `docker/Dockerfile` | `/var/www/html/config/` | `COPY config/ /var/www/html/config/` | WIRED | Line 115 confirmed; matches config_sync_directory resolution |
| `docker/entrypoint.sh` | `config/sync/` | `drush deploy` reads config_sync_directory from settings.php | WIRED | Line 142: `$DRUSH deploy -y`; settings.php is in Docker image via builder stage COPY |
| `config/sync/core.extension.yml` | `html/modules/custom/localnodes_platform/` | Module listed enables it on drush cim | WIRED | Line 81: `localnodes_platform: 0` confirmed |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CFG-01 | 10-01 | settings.php is a committed template with getenv() calls for all environment-specific values | SATISFIED | Template confirmed at `html/sites/default/settings.php`; DB, hash_salt, reverse proxy, trusted hosts all via getenv() |
| CFG-02 | 10-01 | config/sync directory is included in the Docker image so drush deploy can import config | SATISFIED | `COPY config/ /var/www/html/config/` at Dockerfile line 115 |
| CFG-03 | 10-01, 10-03 | config_exclude_modules excludes demo modules; web3 modules are core platform and ship in config/sync | SATISFIED | settings.php excludes only 4 demo modules; web3 modules in core.extension.yml and config/sync/; siwe_login.settings.yml, safe_smart_accounts.settings.yml in config/sync |
| CFG-04 | 10-02 | config/sync is complete — localnodes_platform in core.extension.yml, key.key.gemini_api_key.yml present, demo/web3 modules in correct positions | SATISFIED | All conditions confirmed via grep; 1057 config files in sync directory |
| CFG-05 | 10-02 | Entrypoint uses drush deploy for existing installs instead of config:import --partial | SATISFIED | Zero `config:import --partial` occurrences; `drush deploy -y` at line 142 |
| CFG-06 | 10-01 | Deploy hook scaffold file exists at localnodes_platform.deploy.php | SATISFIED | File exists with correct docblock and drush.org reference |

**No orphaned requirements.** All 6 CFG requirements map to Phase 10 plans and are verified.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.claude/skills/localnodes-coolify-deploy/SKILL.md` | 62 | "Enable Web3 modules (siwe_login...)" listed as something both paths must do, but entrypoint no longer does this (web3 handled by config sync since commit ddcdacb) | Info | Stale documentation; file is gitignored and local-only; no functional impact on deployments |
| `.claude/skills/localnodes-coolify-deploy/SKILL.md` | 176 | "excludes demo **and web3** modules from config sync. These are enabled per-instance by the entrypoint." — factually incorrect; web3 modules are now IN config sync | Info | Stale documentation; file is gitignored and local-only; no functional impact on deployments |

No blocker or warning anti-patterns found in functional files.

---

### Human Verification Required

#### 1. Incremental Config Deploy on Existing Install

**Test:** Deploy a new image to an existing Coolify instance (Cascadia or Boulder) without wiping volumes. Make a trivial config change (e.g., a block label), export via `drush cex`, commit, push, trigger redeploy.
**Expected:** Entrypoint logs show "=== EXISTING INSTALL ===" and "Running drush deploy..."; config change appears in active site without data loss or volume wipe.
**Why human:** Cannot simulate running Docker container against live MariaDB from filesystem inspection.

#### 2. Docker Image Config/Sync Path Resolution

**Test:** Build Docker image locally and verify: `docker run --rm <image> ls /var/www/html/config/sync/ | wc -l`
**Expected:** Shows 1057 (or similar count) of YAML files confirming config/sync was correctly copied to the path that settings.php's `../config/sync` resolves to.
**Why human:** Path arithmetic (`../config/sync` relative to Drupal root) is correct by reasoning but a running container confirms the actual Docker build artifact.

---

### Gaps Summary

No gaps. All 6/6 observable truths verified against actual codebase. All artifacts exist, are substantive (not stubs), and are correctly wired.

**Notable corrections applied during Phase 10** (documented here for completeness):
- Plan 10-03 discovered that web3 modules were incorrectly placed in `config_exclude_modules` by Plan 10-01. Commit `ddcdacb` corrected this: web3 modules moved to `config/sync/core.extension.yml` as core platform modules. The SKILL.md contains two stale sentences from before this correction (lines 62 and 176) but is gitignored and has no functional impact.

---

### Commit Verification

All commits documented in SUMMARY files verified present in git history:
- `dbcacef` — feat(10-01): replace stock settings.php with 12-factor template
- `886dbb1` — feat(10-01): add config/sync to Docker image and deploy hook scaffold
- `790937a` — feat(10-02): fix config/sync gaps — add localnodes_platform, remove demo modules
- `6ba906e` — feat(10-02): simplify entrypoint — use drush deploy, remove settings.php generation
- `ddcdacb` — fix(10): treat web3 modules as core platform, not per-instance

---

_Verified: 2026-03-02T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
