---
phase: 03-permission-aware-retrieval
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: []
autonomous: true
requirements: [PERM-01, PERM-02, PERM-03, PERM-04, PERM-05]
user_setup: []

must_haves:
  truths:
    - "User in Group A cannot retrieve content from Group B"
    - "Anonymous users only see public content"
    - "Community-wide search returns public + community content only"
    - "Group-scoped search only returns content from that group"
    - "Recently removed group members cannot access former group's content"
  artifacts:
    - path: ".planning/phases/03-permission-aware-retrieval/03-VERIFICATION.md"
      provides: "Verification results for all permission scenarios"
  key_links:
    - from: "verification tests"
      to: "PermissionFilterService"
      via: "direct service calls"
      pattern: "applyPermissionFilters|filterResultsByAccess"
    - from: "verification tests"
      to: "Search API query"
      via: "simulated queries"
      pattern: "addCondition"
---

<objective>
Verify permission boundaries through comprehensive testing of adversarial scenarios that attempt to access unauthorized content.

Purpose: Prove the defense-in-depth approach works — no permission leakage under any tested condition.
Output: Verification report confirming all permission requirements are satisfied.
</objective>

<execution_context>
@/Users/proofoftom/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/prooftom/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-permission-aware-retrieval/03-RESEARCH.md
@.planning/phases/03-permission-aware-retrieval/03-01-PLAN.md
@.planning/phases/03-permission-aware-retrieval/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify pre-retrieval group filtering (PERM-01, PERM-05)</name>
  <files>.planning/phases/03-permission-aware-retrieval/03-VERIFICATION.md</files>
  <action>
    Create verification script and run pre-retrieval filter tests via Drush:
    
    Create PHP verification script that tests:
    
    1. **Group membership isolation (PERM-05):**
       - Create mock user with Group A membership only
       - Query with group-scoped filter for Group A
       - Verify results only contain content from Group A
       - Query with group-scoped filter for Group B (user not member)
       - Verify zero results returned
    
    2. **Multi-group user access (PERM-01):**
       - Create mock user with Group A and Group B memberships
       - Query without scope (user's accessible groups)
       - Verify results contain content from A and B only
       - Verify content from Group C (not member) is excluded
    
    3. **Empty membership handling:**
       - Create mock user with no group memberships
       - Query without scope
       - Verify zero results (impossible condition applied)
    
    Run via: `ddev drush php-script verify_permission_filters.php`
    
    Record results in 03-VERIFICATION.md with:
    - Test case description
    - Expected behavior
    - Actual behavior
    - Pass/Fail status
  </action>
  <verify>
    <automated>ddev drush php-eval "return \Drupal::service('social_ai_indexing.permission_filter')->getAccessibleGroupIds(\Drupal\currentUser());"</automated>
    <manual>Review 03-VERIFICATION.md for pass/fail status on all test cases</manual>
  </verify>
  <done>Pre-retrieval group filtering verified — users only retrieve content from groups they can access.</done>
</task>

<task type="auto">
  <name>Task 2: Verify visibility-based filtering (PERM-04)</name>
  <files>.planning/phases/03-permission-aware-retrieval/03-VERIFICATION.md</files>
  <action>
    Extend verification to test content visibility filtering:
    
    1. **Anonymous user filtering:**
       - Simulate anonymous user context
       - Query community-wide
       - Verify only content with visibility='public' returned
       - Verify visibility='community' and 'group_content' excluded
    
    2. **Authenticated community-wide search:**
       - Simulate authenticated user with no group context
       - Query community-wide
       - Verify results include visibility='public' and 'community'
       - Verify visibility='group_content' excluded
    
    3. **Group-scoped override:**
       - Simulate user in Group A with group-scoped query
       - Verify visibility filtering NOT applied (group_id filter is authoritative)
       - Verify group_id filter takes precedence
    
    4. **ContentVisibility processor verification:**
       - Check that content_visibility field is indexed
       - Verify values are correctly extracted from field_content_visibility
    
    Run verification and append results to 03-VERIFICATION.md
  </action>
  <verify>
    <automated>ddev drush search-api:index social_posts --limit 5 && ddev drush php-eval "print_r(\Drupal::entityTypeManager()->getStorage('node')->load(1)->get('field_content_visibility')->value);"</automated>
    <manual>Review visibility test results in 03-VERIFICATION.md</manual>
  </verify>
  <done>Visibility-based filtering verified — community-wide and anonymous access correctly restricted.</done>
</task>

<task type="auto">
  <name>Task 3: Verify post-retrieval defense-in-depth (PERM-02, PERM-03)</name>
  <files>.planning/phases/03-permission-aware-retrieval/03-VERIFICATION.md</files>
  <action>
    Verify post-retrieval access checks catch edge cases:
    
    1. **Entity access validation:**
       - Create mock results array with entity IDs
       - Include entity user cannot access (in different group)
       - Call PermissionFilterService.filterResultsByAccess()
       - Verify unauthorized entity removed from results
    
    2. **Deleted entity handling:**
       - Include entity ID that no longer exists
       - Call filterResultsByAccess()
       - Verify no exception, result silently skipped
    
    3. **Multi-group content edge case:**
       - Content belongs to both Group A (user member) and Group B (user not member)
       - Pre-retrieval filter allows (any-match IN operator)
       - Post-retrieval access check validates entity->access('view')
       - Verify correct behavior per Drupal access rules
    
    4. **Permission change scenario:**
       - User was member of Group A, now removed
       - Query without cached permissions
       - Verify user cannot access Group A content
    
    Append all results to 03-VERIFICATION.md with final summary:
    - Total tests run
    - Tests passed
    - Tests failed (blockers if any)
    - Overall permission system status
  </action>
  <verify>
    <automated>ddev drush php-eval "\$service = \Drupal::service('social_ai_indexing.permission_filter'); \$results = [['drupal_entity_type' => 'node', 'drupal_entity_id' => 999]]; print_r(\$service->filterResultsByAccess(\$results, \Drupal::currentUser()));"</automated>
    <manual>Review final verification summary in 03-VERIFICATION.md</manual>
  </verify>
  <done>Post-retrieval access checks verified — defense-in-depth catches edge cases and prevents leakage.</done>
</task>

</tasks>

<verification>
1. All 5 permission requirements (PERM-01 through PERM-05) tested
2. Pre-retrieval filtering verified for group and visibility constraints
3. Post-retrieval access checks verified for defense-in-depth
4. Edge cases handled (deleted entities, multi-group, permission changes)
5. 03-VERIFICATION.md created with comprehensive test results
</verification>

<success_criteria>
- PERM-01: Pre-retrieval filtering respects Group permissions ✓
- PERM-02: Post-retrieval entity access check provides defense-in-depth ✓
- PERM-03: AI responses contain only authorized content ✓
- PERM-04: Community-wide search surfaces only public content ✓
- PERM-05: Group-scoped queries surface only that Group's content ✓
</success_criteria>

<output>
After completion, create `.planning/phases/03-permission-aware-retrieval/03-03-SUMMARY.md`
</output>
