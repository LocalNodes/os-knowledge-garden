---
phase: 05-user-interface
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-02-27T05:02:16.929Z
---

<current_state>
Phase 05 Plan 05-03 human-verify checkpoint. Tasks 1-2 (CSS + messageStyles) committed. Task 3 (verification) is in progress. Chatbot is now working with correct group scoping after fixing GroupMetadata processor. Citations render as markdown links but are NOT clickable in the UI. All other UI functionality verified working by user.
</current_state>

<completed_work>

- Plan 05-01: Group Context Injection - DONE
- Plan 05-02: Community AI Search Page - DONE
- Plan 05-03 Task 1: CSS file for search page, citations, AI visual distinction - DONE
- Plan 05-03 Task 2: messageStyles JSON for DeepChat AI bubble distinction - DONE
- Plan 05-03 Task 3: Human verification - PARTIALLY DONE
  - Chatbot working ✓
  - Group-scoped filtering working ✓ (node 12 in group 3 correctly returned)
  - Citations render as markdown links but NOT clickable ✗
  - AI visual distinction (messageStyles) - untested
  - Community search page /search/ai - untested

Major bug fixes this session:
- GroupMetadata processor: GroupContent→GroupRelationship class (Group 2.x rename)
- GroupMetadata processor: Added type LIKE '%group_node-%' filter to avoid user membership collisions
- Consolidated from entity 'groups' field to processor 'group_id' field in Search API index
- Updated PermissionFilterService to query group_id instead of groups
- Removed cardinality override hack (no longer needed)
- MilvusProvider (contrib): Fixed if→elseif bug + getClient()->getPluginId()→getPluginId()
- Search API + AI Search config updated via update script (group_id replaces groups)
- Both social_posts and social_comments reindexed with correct group_id values in Milvus
</completed_work>

<remaining_work>

- Complete human-verify checkpoint: test citations clickability, AI distinction, /search/ai page
- Citations not clickable — investigate: likely CommonMark→HTML rendering or DeepChat shadow DOM issue
- MilvusProvider patch: contrib fix (elseif + getPluginId) needs formal patch file — currently only in vendor, will be lost on composer update
- Formalize contrib patching workflow (user requested)
- Design AI agent tools for scope switching: "search_group" and "search_community" tools so LLM can choose scope based on user intent (user asked about broader community from group context, chatbot stayed group-scoped)
- Milvus Docker restart policy: currently "no", should be "unless-stopped" — .ddev/docker-compose.ai.yaml is #ddev-generated so needs separate override file
- After checkpoint approved: create 05-03-SUMMARY.md
- After all plans complete: verify_phase_goal (gsd-verifier), update_roadmap, offer_next
</remaining_work>

<decisions_made>

- Consolidated on processor-generated `group_id` field instead of entity computed `groups` field — processor fields reliably write to Milvus metadata, entity computed fields don't
- Removed cardinality override hack — no longer needed with group_id processor field
- Added `type LIKE '%group_node-%'` filter in GroupMetadata to avoid entity_id collision between node content and user memberships
- MilvusProvider fix (contrib): changed `if` to `elseif` for IN operator check, and `getClient()->getPluginId()` to `getPluginId()` — both clear upstream bugs
- Provider switched to Gemini (gemini-2.5-flash for chat, gemini-embedding-001 for embeddings, 3072 dimensions) — user changed this between sessions
- User wants AI agent tools for dynamic scope switching rather than hardcoded group scope
</decisions_made>

<blockers>
- Citations not clickable in DeepChat — needs investigation (CommonMark rendering vs shadow DOM)
- MilvusProvider patch will be lost on next composer install/update — needs formal patch
- Other contrib changes (AgentRunner.php, DeepChatApi.php, gemini_provider api_defaults.yml) also uncommitted/unpatched
</blockers>

<context>
The execute-phase orchestrator originally spawned 3 executor agents for waves 1-3. We're at the Plan 05-03 human-verify checkpoint. The user tested the chatbot and it works with correct group scoping. However: (1) citations aren't clickable, (2) when user asked about "the entire community" from group context, the chatbot still filtered to the group. User wants AI agent tools for scope switching.

After the checkpoint is approved, the execute-phase workflow should:
1. Create 05-03-SUMMARY.md
2. Run verify_phase_goal (gsd-verifier)
3. Update roadmap
4. Offer next phase

The user also raised two workflow improvements:
1. Formalize contrib patching (patches dir, composer patches plugin)
2. AI agent tools for dynamic search scope (group vs community)

There are also uncommitted contrib changes in git (AgentRunner.php, DeepChatApi.php, gemini_provider) that predate this session and need to be addressed (patches or commits).
</context>

<next_action>
1. Investigate citations not clickable — check if CommonMark is converting markdown to HTML in DeepChatApi response, check DeepChat shadow DOM rendering
2. Formalize contrib patching workflow — create patches/ directory, set up composer-patches plugin, create patch for MilvusProvider fix
3. Design and implement AI agent tools for scope switching (search_group / search_community)
4. Complete Phase 05-03 verification checkpoint
</next_action>
