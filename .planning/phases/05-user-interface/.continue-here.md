---
phase: 05-user-interface
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-02-27T09:23:27.639Z
---

<current_state>
Phase 05-03 verification was proceeding, but user discovered RAG pipeline issues during testing. Pivoted to debugging RAG retrieval. Multiple fixes applied but new issues emerged. Currently: Comments are not getting citation metadata (empty title/URL/type), and agent is asking which index to use instead of auto-searching both.
</current_state>

<completed_work>

- Plan 05-01: Group Context Injection - DONE
- Plan 05-02: Community AI Search Page - DONE
- Plan 05-03 Task 1: CSS file for search page - DONE
- Plan 05-03 Task 2: messageStyles JSON for DeepChat - DONE
- Plan 05-03 Task 3: Human verification - WAS proceeding until RAG issues found

**RAG Debugging Session Fixes:**
- Fixed Deepseek→Gemini configuration in ai_assistant and search_api configs
- Re-indexed social_posts (10 items) and social_comments (13 items) with Gemini embeddings
- Updated CitationMetadata.php to use anchor URLs for comments (`/node/12#comment-5`)
- Patched SearchApiAiSearchBackend.php to include citation_url/title/type in output_fields
- Edited RagTool.php to include citation metadata in output string
- Changed citation fields from `contextual_content` to `attributes` indexing option (required for Milvus metadata storage)
- Updated agent to allow both social_posts AND social_comments indexes
- Updated agent instructions to search both indexes for comments/discussions
</completed_work>

<remaining_work>

1. **Fix comment citation fields** - Comments are not getting citation metadata populated:
   - `citation_title`: empty
   - `citation_url`: empty
   - `citation_type`: empty
   - The CitationMetadata processor might not be firing for comments, or fields aren't being written

2. **Fix agent index selection** - Agent is asking "which index to search" instead of auto-selecting:
   - Changed from `force_value` to `allow_list` but agent still asks
   - May need to revert to `force_value` with just `social_posts` for now, or fix the prompt

3. **Complete Phase 05-03 verification** once RAG works:
   - Verify citations are clickable links with correct URLs
   - Verify comment citations link to anchors (#comment-5)
   - Verify AI visual distinction
   - Verify /search/ai page

4. **Create 05-03-SUMMARY.md** after verification passes

5. **Run phase verification** (gsd-verifier)
</remaining_work>

<decisions_made>

- Citation fields must use `indexing_option: attributes` (not `contextual_content`) to be stored in Milvus metadata - only `attributes` fields are written to vector DB metadata
- Comment URLs should use anchor format (`/node/12#comment-5`) for direct linking - requires constructing URL from parent entity URL + comment ID fragment
- Agent should search both indexes when looking for comments/discussions - updated instructions and tool_usage_limits
- RagTool output format changed to include Source: [Title](URL) and Type: lines after content
</decisions_made>

<blockers>
- **Comment citation fields empty**: CitationMetadata processor may not be firing for comments, or the fields aren't being extracted properly. Debug needed.
- **Agent asking for index**: The `allow_list` action may not work as expected for auto-selection. May need different approach.
</blockers>

<context>
The RAG pipeline was working before the session (user confirmed chatbot was finding comments). After we made configuration changes (Gemini, reindexing, patches), the pipeline broke. We fixed several issues but introduced new ones. The root cause might be:

1. The CitationMetadata processor isn't being triggered for comments
2. The processor is triggered but the addCommentCitationFields method has a bug
3. The fields are set but not being written to Milvus

Need to debug the CitationMetadata processor for comments specifically. The `allow_list` action for index selection also needs investigation - the agent should just pick an index automatically.
</context>

<next_action>
1. Debug why comment citation fields are empty - check if CitationMetadata processor is enabled for social_comments index and if addCommentCitationFields() is being called
2. Consider reverting agent to single index (social_posts only) as temporary fix while debugging
3. Once citation fields work, test full RAG flow again
</next_action>
