---
phase: 06-create-demo-content-for-localnodes-xyz-based-on-social-demo-module
task: boulder-content-verified
total_tasks: 6
status: in_progress
last_updated: 2026-02-28T11:10:16.193Z
---

<current_state>
Boulder demo content is INSTALLED AND WORKING on a fresh database. Plans 06-04 (scaffolding) and 06-05 (YAML content) were already completed in a prior session (module + content files exist). Plan 06-06 (install & verify) was effectively done manually this session. The Cascadia plan 06-03 has NOT been run yet (localnodes_demo content not installed).

Fresh site install was done this session: `ddev drush si social` → enable modules → install boulder_demo content.
</current_state>

<completed_work>

- Plan 06-01: Cascadia module scaffolding - Done (prior sessions)
- Plan 06-02: Cascadia YAML content - Done (prior sessions)
- Plan 06-03: Cascadia install & verify - NOT DONE (skipped — user wanted to test Boulder first)
- Plan 06-04: Boulder module scaffolding - Done (module + 11 plugins exist and work)
- Plan 06-05: Boulder YAML content - Done (11 YAML files exist, all content installs cleanly)
- Plan 06-06: Boulder install & verify - Effectively done manually this session
- Boulder RESEARCH created and plan-checked (both passed)
- Topic type "Resource" renamed to "Research" in DB and install hook
- Added `hook_install()` to localnodes_demo.install so topic type changes persist on fresh installs
- Added `localnodes_demo_update_10002` for existing sites that already ran 10001 with "Resource"
- Cleaned up 393 orphaned Message entities from previous install/remove cycles
</completed_work>

<remaining_work>

- Plan 06-03: Install localnodes_demo (Cascadia) content and verify — this is for the SECOND instance
- Mark plans 06-04, 06-05, 06-06 as complete in ROADMAP.md
- Update STATE.md with session decisions
- Commit the localnodes_demo.install hook_install fix and Resource→Research rename
- The two instances (Cascadia + Boulder) need to be set up as separate sites to simulate the network
</remaining_work>

<decisions_made>

- Renamed topic type "Resource" to "Research" — user preferred it for bioregional community context
- Added `hook_install()` to localnodes_demo.install — update hooks don't run on fresh installs, so the topic type setup (remove "Content", add "Question" + "Research") needs to happen at install time too
- Added `update_10002` to rename "Resource" → "Research" for sites that already ran update_10001
- Helper function `_localnodes_demo_update_topic_types()` shared between hook_install and update hooks
- boulder_demo module fully exists with content — plans 04/05 were completed in a prior untracked session
- Fresh site install was needed to clear orphaned messages and stale state from multiple install/remove cycles
</decisions_made>

<blockers>
- None currently
</blockers>

<context>
The user is building two LocalNodes instances to demo the network:
1. Boulder (tested and working this session)
2. Cascadia (content exists in localnodes_demo module, not yet installed on its own instance)

The boulder_demo content installed cleanly: 12 users, 5 groups, 10 topics (all 5 types including Research + Question), 18 posts, 14 comments, 8 events, 9 likes, 10 enrollments.

Key topic types confirmed: Blog, Dialog, News, Question, Research (tid 1,3,4,5,6 — "Content" tid 2 deleted).

The localnodes_demo.install file has uncommitted changes (hook_install + update_10002 + Resource→Research fix in update_10001).
</context>

<next_action>
Start with: Commit the localnodes_demo.install changes, then update ROADMAP.md to mark Boulder plans (04, 05, 06) as complete. Then decide whether to set up the Cascadia instance as a separate DDEV project or handle 06-03 differently.
</next_action>
