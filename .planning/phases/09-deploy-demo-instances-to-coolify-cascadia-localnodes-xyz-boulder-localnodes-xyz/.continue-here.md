---
phase: 09-deploy-demo-instances-to-coolify-cascadia-localnodes-xyz-boulder-localnodes-xyz
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-02T09:37:07.227Z
---

<current_state>
Provisioning workflow fully tested and working. Both existing instances healthy.
Next: run 09-03 verification plan on Cascadia and Boulder, then close out Phase 09.
</current_state>

<completed_work>

## This session (2026-03-02)

### Provisioning workflow tested end-to-end
- Set GitHub secrets: `COOLIFY_API_TOKEN`, `GEMINI_API_KEY`
- Ran `provision-instance.yml` with subdomain "test-provision"
- First run failed at "Set domain routing" — `-sf` curl flag hid actual error
- Root cause: race condition (app not ready for PATCH immediately after creation)

### Fixed 5 API endpoint bugs
1. Deploy endpoint: `POST /applications/{uuid}/deploy` (404) → `GET /deploy?uuid=...&force=true`
2. Deploy response path: `.data.deployments[0]` → `.deployments[0]` (no `.data` wrapper in raw API)
3. Deployment status path: `.data.status` → `.status` (flat object at root)
4. Applications list path: `.data[]` → `.[]` (plain array, not wrapped)
5. Env var updates: simplified from list→find UUID→PATCH individual to `PATCH /envs` by key directly

### Additional fixes
- Added 5s delay between app creation and domain PATCH (race condition)
- Removed `-sf` curl flags throughout for error visibility
- Added proper HTTP code checking with error output on domain PATCH
- Increased health check timeout from 5min to 10min
- Redacted Coolify API token from committed continue-here file

### Second run: full success
- All 9 workflow steps passed
- Build: 30s, Drupal install: 3m20s, Total: 4m19s
- test-provision.localnodes.xyz verified HTTP 200
- Test instance deleted after verification

## Both existing instances
- **Cascadia:** `pok0ckskksok0sgsokwk8ccw` — running:healthy, https://cascadia.localnodes.xyz
- **Boulder:** `bcokg04wc0kk440gw0ooosgw` — running:healthy, https://boulder.localnodes.xyz

</completed_work>

<remaining_work>

## Execute 09-03 verification plan
- Full verification checklist for both Cascadia and Boulder
- HTTP, SSL, content, chatbot, search, container health
- Human verification checkpoint (browser testing)
- Write 09-03-SUMMARY.md
- Run phase verifier to close out Phase 09

## Future enhancements (post-phase)
- Add teardown workflow (delete instance + volumes)
- Cloudflare Worker wrapper for non-GitHub user self-service
- Wildcard DNS documentation
- `demo_module: custom` option for blank instances

</remaining_work>

<decisions_made>
- Coolify raw API has NO `.data` wrapper on responses (MCP tool adds it) — always test with raw curl
- `PATCH /applications/{uuid}/envs` works by key directly (no need to list+find UUID first)
- Deploy uses generic `GET /deploy?uuid=...` not per-app `POST /applications/{uuid}/deploy`
- 5s delay between create and PATCH is sufficient for race condition
- Health check timeout of 10min covers worst-case Drupal install time
</decisions_made>

<context>
Key UUIDs:
- Cascadia app: pok0ckskksok0sgsokwk8ccw
- Boulder app: bcokg04wc0kk440gw0ooosgw
- Coolify project: n0sc0wcog8k4kkc48k0k0wo0
- Coolify server: q4okokcg8occ88w00c4kg0sw

GitHub secrets configured: COOLIFY_API_TOKEN, GEMINI_API_KEY
Coolify version: v4.0.0-beta.463

Commits this session:
- e76eb24: fix Coolify API endpoints and response paths
- 60e18a4: add error visibility and race condition delay
- 5c135f6: update continue-here with test results
</context>

<next_action>
1. Read .planning/phases/09-.../09-03-PLAN.md for the verification checklist
2. Run automated checks (HTTP, SSL, container health) on both instances
3. Prompt user for browser verification checkpoint
4. Write 09-03-SUMMARY.md
5. Run phase verifier to close Phase 09
</next_action>
