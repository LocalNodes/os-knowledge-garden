---
phase: 09-deploy-demo-instances-to-coolify-cascadia-localnodes-xyz-boulder-localnodes-xyz
task: 3
total_tasks: 3
status: pivoting
last_updated: 2026-03-01T08:30:00.000Z
---

<current_state>
Phase 09 PIVOTING — abandoning the separate open-social-coolify deploy repo approach in favor of a clean, self-contained docker-compose setup in the main repo (LocalNodes/os-knowledge-garden).

Design approved: docs/plans/2026-03-01-clean-docker-compose-deploy-design.md
</current_state>

<completed_work>

- Plans 09-01 and 09-02 are now obsolete (they targeted the old deploy repo approach)
- Design brainstorming complete — Approach C (CI artifact) approved
- Design doc written and saved

</completed_work>

<remaining_work>

## New approach: CI Artifact + Docker Compose in main repo

The old deploy repo (proofoftom/open-social-coolify) is being replaced. New work:

1. **Create deployment files in this repo:**
   - docker/Dockerfile (multi-stage: composer install + patches → lean runtime)
   - docker/Dockerfile.solr (Solr 9 + configset)
   - docker/entrypoint.sh (clean first-boot vs restart logic)
   - docker/solr-config/ (copy from DDEV configset)
   - docker-compose.yml (4-service stack, opensocial pulls from GHCR)

2. **Set up GitHub Actions CI:**
   - .github/workflows/build-image.yml
   - Build multi-stage Dockerfile, push to ghcr.io/localnodes/os-knowledge-garden
   - Tags: latest + git SHA

3. **Push to LocalNodes/os-knowledge-garden:**
   - Ensure this repo is properly set up as a remote
   - Push all Docker/CI files

4. **Reconfigure Coolify:**
   - Delete old apps (j8osgcos8scos0ccw44wgo00, f8ccwks04oso408ko44wwkoc)
   - Create new apps pointing to LocalNodes/os-knowledge-garden
   - Set per-instance env vars
   - Map domains: cascadia.localnodes.xyz, boulder.localnodes.xyz

5. **Verify deployment:**
   - Both sites load with correct demo content
   - AI chatbot functional
   - SSL certs active

</remaining_work>

<decisions_made>

- Approach C: CI builds Docker image → pushes to GHCR → Coolify pulls pre-built image
- Registry: ghcr.io/localnodes/os-knowledge-garden (public, free)
- One image for both instances, DEMO_MODULE env var at runtime differentiates content
- DDEV stays for local dev, Docker files are production-only
- Solr is the only service that builds on Coolify (trivial, just copies configset)

</decisions_made>

<blockers>
None — clean start with approved design.
</blockers>

<context>
Repo: LocalNodes/os-knowledge-garden (https://github.com/LocalNodes/os-knowledge-garden)
Design: docs/plans/2026-03-01-clean-docker-compose-deploy-design.md
Old deploy repo: proofoftom/open-social-coolify (localnodes branch) — being replaced
Coolify project UUID: n0sc0wcog8k4kkc48k0k0wo0
</context>

<next_action>
1. Next session should invoke /gsd:plan-phase or create an implementation plan from the design doc
2. The writing-plans skill should be used to break the design into executable tasks
3. Start with the Dockerfile and entrypoint.sh, then CI, then Coolify reconfiguration
</next_action>
