---
phase: 09-deploy-demo-instances-to-coolify-cascadia-localnodes-xyz-boulder-localnodes-xyz
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-02T11:39:27.155Z
---

<current_state>
Phase 09 nearly complete. All instances deployed and verified. Deployment audit done, fixes applied. Blank instance support added and tested. One remaining item: tear down blank-test instance, write 09-03-SUMMARY.md, run phase verifier.
</current_state>

<completed_work>

## Previous sessions
- 09-01: Docker/CI infrastructure (Dockerfile, docker-compose, GitHub Actions build workflow)
- 09-02: Coolify app creation, env var config, deployment automation (provision-instance.yml)

## This session (2026-03-02)

### Portland re-provisioned correctly
- Found DEMO_MODULE was `localnodes_demo` (wrong) — provisioning workflow ran with default selection
- Deleted Portland app entirely (volumes wiped), re-provisioned with `portland_demo`
- Portland UUID: isog8s808s4gkgcgw8sc880g — verified working

### Blank instance support (DEMO_MODULE=none)
- Updated entrypoint to skip demo module enable + content loading when DEMO_MODULE=none
- Added `none` option to provisioning workflow
- Deployed blank-test instance — verified working (3m38s, faster than with content)

### Env var dedup fix
- Root cause: Coolify auto-generates env vars from docker-compose ${VAR} references, creating duplicates
- Fix: Increased wait from 5s to 15s, two-pass dedup in provisioning workflow
- Verified: Pass 1 catches all 13 dupes, pass 2 confirms clean

### Deployment audit fixes (from external agent review)
- **Solr config key bug**: settings.php targeted `search_api.server.solr` but actual config ID is `search_api.server.social_solr` — fixed
- **Qdrant config key bug**: settings.php used `hostname` but YAML key is `host` — fixed
- **Chatbot permission bandaid**: Moved `access deepchat api` from entrypoint `drush role:perm:add` to `localnodes_platform_install()` hook

### AgentRunner null crash patch
- `AgentRunner::solve()` returns null on empty Qdrant collections
- `ChatMessage` constructor crashes with TypeError (string required, null given)
- Created patch: `patches/ai-assistant-api-agent-runner-null-response.patch`
- Added to composer.json patches for drupal/ai

</completed_work>

<remaining_work>

## Close out Phase 09
1. Tear down blank-test instance (test complete, no longer needed)
2. Write 09-03-SUMMARY.md
3. Run phase verifier
4. Update STATE.md and ROADMAP.md

## Memory updates needed
- Update MEMORY.md with:
  - Portland UUID: isog8s808s4gkgcgw8sc880g
  - Blank instance support (DEMO_MODULE=none)
  - Env var dedup root cause and fix
  - Config key fixes (social_solr, host)
  - AgentRunner null patch

</remaining_work>

<decisions_made>
- Delete Portland app entirely rather than just updating env vars — ensures clean state with no wrong content in DB
- DEMO_MODULE=none skips demo module enable AND content loading (not just content)
- Env var duplicates are from Coolify compose parsing, not preview deployments (though could be related)
- Solr/Qdrant settings.php overrides were silently broken (wrong config keys) — fixed in settings.php generation
- Chatbot permission belongs in hook_install() since role entities already exist from install profile (can't use config/install)
- AgentRunner null response: simple `?? ''` coalesce is sufficient (upstream bug in drupal/ai)
</decisions_made>

<blockers>
- Chatbot on blank instances is slow (~30-60s) even with null fix — inherent to RAG pipeline making Gemini API calls. Not actionable without upstream changes to short-circuit on empty indexes.
</blockers>

<context>
Commits this session:
- 7782ff0: blank instance support + env var dedup fix
- 00e61b6: Solr/Qdrant config key fixes + chatbot permission migration
- c084f62: AgentRunner null response patch

Active instances:
- Cascadia: pok0ckskksok0sgsokwk8ccw (running)
- Boulder: bcokg04wc0kk440gw0ooosgw (running)
- Portland: isog8s808s4gkgcgw8sc880g (running)
- blank-test: pksww8w8skgog8g4s8k4cgcc (running — tear down when done)

Audit findings NOT acted on (valid but not urgent):
- Heavy entrypoint work on every restart (config imports, reindex, cron)
- Unsafe compose defaults (weak passwords) — mitigated by provisioning workflow
- Reverse proxy 0.0.0.0/0 trust — low risk behind Traefik
- Non-deterministic :latest image tags — fine for pre-launch
</context>

<next_action>
1. Tear down blank-test instance via Coolify API
2. Write 09-03-SUMMARY.md summarizing all Phase 09 work
3. Run phase verifier to close Phase 09
4. Update MEMORY.md with session learnings
</next_action>
