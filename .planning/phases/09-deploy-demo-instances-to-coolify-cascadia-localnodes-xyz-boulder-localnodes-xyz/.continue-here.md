---
phase: 09-deploy-demo-instances-to-coolify-cascadia-localnodes-xyz-boulder-localnodes-xyz
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-01T07:03:09.611Z
---

<current_state>
Phase 09 execution — plans 09-01 and 09-02 complete (SUMMARYs exist). Plan 09-03 (verification) is in progress.

Both Coolify apps are deployed and containers are `running:healthy`. The site is serving pages on the auto-generated UUID domains but NOT on the custom domains (cascadia.localnodes.xyz / boulder.localnodes.xyz). Domain routing is the primary remaining issue.
</current_state>

<completed_work>

- Plan 09-01: Deploy repo update — DONE (SUMMARY exists, committed)
- Plan 09-02: Create Coolify apps and deploy — DONE (SUMMARY exists, committed)
- Plan 09-03 Task 1 (partial): Container health verified — both apps `running:healthy`
  - Fixed docker_compose_location from `.yaml` to `.yml` (was causing deploy failure)
  - Fixed Boulder missing FQDN
  - Updated docker_compose_domains to custom domains (cascadia/boulder.localnodes.xyz)
  - Redeployed 3 times total (initial fail, compose fix, domain fix)
  - Both apps now running with all 4 containers healthy (opensocial, mariadb, solr, qdrant)
</completed_work>

<remaining_work>

- Plan 09-03 Task 1 (remaining): Fix domain routing — custom domains return 503, UUID domains work
- Plan 09-03 Task 2: Verify domain accessibility, demo content, SSL certs via HTTP
- Plan 09-03 Task 3: Human verification checkpoint (visit both sites, test AI chatbot, verify demo content)
- Phase verification (after 09-03)
</remaining_work>

<decisions_made>

- Used Coolify API directly (curl) for fields the MCP tool doesn't expose (docker_compose_location, docker_compose_domains)
- Coolify API token: `krburCtf3JwaJo7FgicNV0dQDhDiTr7R3qS2xCAb3ed52456` (user-provided)
- Previous token from MCP process env: `SyaPFV74HT5ISjNrrKRiBQe9xtkMynou14txkw49642c6d46` (still works for MCP)
- API base: `https://coolify.localnodes.xyz/api/v1`
</decisions_made>

<blockers>

- DOMAIN ROUTING (PRIMARY): Both sites return 503 on custom domains (cascadia.localnodes.xyz, boulder.localnodes.xyz) but work on UUID domains (opensocial-sw08occo8so0g4okkw0w8goc.localnodes.xyz, opensocial-lwkkk4s00wokowc4c8o8k0sg.localnodes.xyz)
  - Root cause: For Docker Compose apps, Coolify generates per-container Traefik labels from `docker_compose_domains`. Even after updating that field via API, redeployment regenerates it back to the UUID-based domain.
  - The `custom_labels` field (base64-encoded) has correct `Host('cascadia.localnodes.xyz')` rules but these appear to be application-level, not applied to the opensocial container.
  - Possible fixes to try:
    1. Set docker_compose_domains via Coolify UI (not API) — may stick better
    2. Add explicit Traefik labels in docker-compose.yml in the deploy repo
    3. Use Coolify's SERVICE_FQDN_OPENSOCIAL env var pattern to control domain
    4. Check if Cloudflare DNS is configured for cascadia/boulder subdomains (the 503 comes through Cloudflare)
- Minor: `group_node:book` plugin not found error — Book module not enabled but Group references it. Non-blocking.
</blockers>

<context>
The sites ARE working — you can verify by visiting the UUID domains directly:
- https://opensocial-sw08occo8so0g4okkw0w8goc.localnodes.xyz (Cascadia)
- https://opensocial-lwkkk4s00wokowc4c8o8k0sg.localnodes.xyz (Boulder)

The domain routing problem is likely a combination of:
1. Coolify regenerating docker_compose_domains on each deploy (overwriting custom domain)
2. Possibly missing Cloudflare DNS records for cascadia/boulder subdomains

The cleanest fix is probably adding explicit Traefik labels in the docker-compose.yml in the deploy repo, or using Coolify UI to set the domain mapping.

Coolify app UUIDs:
- Cascadia: sw08occo8so0g4okkw0w8goc
- Boulder: lwkkk4s00wokowc4c8o8k0sg

Restart was in progress when session ended (deployment UUIDs: n8c00ggwo448s0ow40c8ssgc, ikokog44sss08wwokwo0coss).
</context>

<next_action>
1. Check if Cloudflare DNS has A/CNAME records for cascadia.localnodes.xyz and boulder.localnodes.xyz
2. If DNS is fine, fix domain routing by either:
   a. Using Coolify UI to set opensocial service domain to cascadia/boulder.localnodes.xyz
   b. OR adding explicit Traefik labels in docker-compose.yml in the deploy repo (proofoftom/open-social-coolify localnodes branch)
3. Once domains work, complete 09-03 verification tasks and human checkpoint
4. Create 09-03-SUMMARY.md
5. Run phase verification
</next_action>
