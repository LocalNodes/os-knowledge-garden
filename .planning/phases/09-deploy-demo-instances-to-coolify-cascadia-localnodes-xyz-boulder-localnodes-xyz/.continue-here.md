---
phase: 09-deploy-demo-instances-to-coolify-cascadia-localnodes-xyz-boulder-localnodes-xyz
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-03-02T07:16:42.256Z
---

<current_state>
Phase 09, Plan 02 nearly complete. Both apps deployed and running:healthy. Three remaining issues being worked:
1. Inline [N] chatbot citations need JS linkification (code written, deployed, untested)
2. Custom domains not routed — `docker_compose_domains` in Coolify still points to internal auto-generated domains
3. Sources section line separation — markdown list format in system prompt, needs verification
</current_state>

<completed_work>

## Plan 09-01: Docker/CI Infrastructure (COMPLETE)
- All 3 tasks done, SUMMARY.md created

## Plan 09-02: Coolify App Creation (90% COMPLETE)
- Apps created and running:healthy
  - Cascadia: bkc8kswc4g4gck008w0kw8k4
  - Boulder: bcokg04wc0kk440gw0ooosgw
- Demo content loaded correctly per instance
- AI chatbot functional, AI overview functional, search works
- Entrypoint now runs `drush config:import --partial` on existing installs (config YAML changes take effect without volume wipe)
- System prompt updated: plain [1] inline + markdown list sources format
- JS post-processing added to deepchat-links.js: parses Sources section, linkifies inline [N] with href + title attribute
- Env vars for SERVICE_FQDN/URL updated via API (but get overwritten by docker_compose_domains — see blockers)

## Bug fixes applied (commits on main):
1. 6eb3411: plugin ID fix + healthcheck
2. 7b32efb: Solr preserve, image perms, auto re-index
3. 2514e83: chatbot API permission fix
4. a913e06: citation URL fix (drush --uri)
5. d876ff1: numbered citation format in system prompt
6. 7f1cfad + 57e1816: citation links same tab
7. 2f5afff: citation linkification + list sources in prompt
8. d5734d8: entrypoint config:import --partial for existing installs
9. f8f8620: JS linkification of inline citations + prompt simplification

## Key learnings captured:
- Memory + skill updated with: Drupal config/install gotcha, CI/deploy race condition, SSH debugging instructions
- `docker_compose_domains` controls Coolify's auto-generated SERVICE_FQDN/URL env vars (overrides manual env var changes)

</completed_work>

<remaining_work>

## Immediate: Fix custom domain routing
- `docker_compose_domains` must be updated in Coolify UI for both apps:
  - Cascadia: opensocial service → `https://cascadia.localnodes.xyz`
  - Boulder: opensocial service → `https://boulder.localnodes.xyz`
- This is NOT possible via MCP tools or API update action — must be done in Coolify web UI
- Once set, redeploy both apps — Coolify will auto-generate correct SERVICE_FQDN/URL env vars
- Then re-index will use correct base URLs for citation links

## After domain fix: Verify chatbot citation behavior
- Test inline [N] are clickable links with title hover text (JS linkification)
- Test sources section renders each source on its own line (markdown list)
- Test citation URLs use correct custom domain (not internal Coolify domain)

## Then: Create 09-02-SUMMARY.md

## Then: Execute 09-03 verification plan + phase verifier

</remaining_work>

<decisions_made>
- LLM-prompt approach for inline citation links abandoned — LLMs don't reliably produce [[1]](url "Title") nested bracket markdown
- JS post-processing approach adopted instead — parse Sources section, linkify plain [N] text (matches AI Overview pattern)
- Entrypoint now runs `drush config:import --partial` from custom module config dirs on existing installs
- System prompt reverted to plain [1] inline format (simpler for LLM) with markdown list for sources section
- CI must complete before triggering Coolify deploy (race condition: both triggered by git push)
</decisions_made>

<blockers>
- **docker_compose_domains**: Coolify auto-generates SERVICE_FQDN_OPENSOCIAL and SERVICE_URL_OPENSOCIAL from this field, overwriting any manual env var updates. The MCP tool's `application update` action doesn't expose `docker_compose_domains`. Must be set via Coolify web UI.
</blockers>

<context>
Key UUIDs:
- Cascadia app: bkc8kswc4g4gck008w0kw8k4
- Boulder app: bcokg04wc0kk440gw0ooosgw
- Coolify project: n0sc0wcog8k4kkc48k0k0wo0
- Coolify server: q4okokcg8occ88w00c4kg0sw

SSH access available: root@localnodes.xyz port 22
Container debugging: `docker exec <container> drush config:get <config_name> <key>`

Boulder env vars are already correctly set (boulder.localnodes.xyz) — only needs docker_compose_domains fix in UI.
</context>

<next_action>
1. User sets docker_compose_domains in Coolify UI for both apps (opensocial service → custom domain)
2. Redeploy both apps (wait for CI if new commits)
3. Verify chatbot citations: inline links, title text, sources on own lines, correct domain URLs
4. Write 09-02-SUMMARY.md
5. Execute 09-03 verification plan
</next_action>
