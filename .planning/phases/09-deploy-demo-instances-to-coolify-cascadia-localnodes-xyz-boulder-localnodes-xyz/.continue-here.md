---
phase: 09-deploy-demo-instances-to-coolify-cascadia-localnodes-xyz-boulder-localnodes-xyz
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-03-02T07:35:55.590Z
---

<current_state>
Phase 09, Plan 02 blocker RESOLVED. The `docker_compose_domains` field CAN be set programmatically via Coolify API — previous session incorrectly concluded it required UI-only changes.

Key discovery this session: `PATCH /api/v1/applications/{uuid}` accepts `docker_compose_domains` as an array of `{name, domain}` objects. This controls the magic env vars (`SERVICE_FQDN_*`, `SERVICE_URL_*`) injected into all containers.

Cascadia's Traefik labels were updated via MCP `fqdn` field (routing works), but `docker_compose_domains` and container env vars still point to old auto-generated domain. Need direct API call to fix.
</current_state>

<completed_work>

## Plan 09-01: Docker/CI Infrastructure (COMPLETE)
- All tasks done, SUMMARY.md created

## Plan 09-02: Coolify App Creation (95% COMPLETE)
- Apps created and running:healthy
  - Cascadia: bkc8kswc4g4gck008w0kw8k4
  - Boulder: bcokg04wc0kk440gw0ooosgw
- Demo content loaded correctly per instance
- AI chatbot functional, AI overview functional, search works
- All bug fixes from previous sessions applied (9 commits)

## This session discoveries:
1. **Boulder already has correct docker_compose_domains** (`boulder.localnodes.xyz`) — was fixed at some point
2. **Cascadia still has auto-generated domain** (`opensocial-bkc8kswc4g4gck008w0kw8k4.localnodes.xyz`)
3. **MCP `fqdn` update for Cascadia succeeded** — Traefik labels now route `cascadia.localnodes.xyz` correctly
4. **BUT `docker_compose_domains` not updated by `fqdn`** — container env vars still wrong
5. **API research via Context7 confirmed**: `PATCH /api/v1/applications/{uuid}` accepts `docker_compose_domains: [{name: "opensocial", domain: "https://cascadia.localnodes.xyz"}]`
6. **MCP Coolify tool (`@masonator/coolify-mcp@2.7.3`) doesn't expose `docker_compose_domains`** — needs direct API call
7. **Coolify API base URL not yet determined** — tried `localnodes.xyz` (307 redirect to www), `www.localnodes.xyz` (404), port 8000 (unreachable). Need to find the correct Coolify admin URL.

## LocalNodes-as-a-Service vision noted
User wants automated provisioning: users pick a subdomain, system creates a new LocalNodes instance at `{subdomain}.localnodes.xyz`. The `docker_compose_domains` API makes this fully scriptable.

</completed_work>

<remaining_work>

## Immediate: Fix Cascadia docker_compose_domains
1. Find correct Coolify API base URL (check Coolify admin panel URL or server config)
2. Call `PATCH /api/v1/applications/bkc8kswc4g4gck008w0kw8k4` with:
   ```json
   {
     "docker_compose_domains": [
       {"name": "opensocial", "domain": "https://cascadia.localnodes.xyz"}
     ]
   }
   ```
3. Redeploy Cascadia so container env vars regenerate with correct domain
4. Verify `SERVICE_FQDN_OPENSOCIAL` and `SERVICE_URL_OPENSOCIAL` now show `cascadia.localnodes.xyz`

## After domain fix: Verify chatbot citations
- Test inline [N] are clickable links with title hover text (JS linkification)
- Test sources section renders each source on its own line
- Test citation URLs use correct custom domain

## Then: Write 09-02-SUMMARY.md (update existing draft)

## Then: Execute 09-03 verification plan + phase verifier

## Future: Update Coolify skill and memory
- Document `docker_compose_domains` API approach
- Note LocalNodes-as-a-Service provisioning pattern
- Remove incorrect "UI-only" blocker from docs

</remaining_work>

<decisions_made>
- `docker_compose_domains` is the correct API field for setting custom domains on docker-compose services (controls SERVICE_FQDN/URL magic env vars)
- MCP tool `fqdn` only updates Traefik routing labels, NOT container env vars for docker-compose apps
- For automated provisioning, the full flow would be: create app via API → set docker_compose_domains → set env vars (DEMO_MODULE, DRUPAL_SITE_NAME, etc.) → deploy
- User provided temporary API key for testing: should be deleted after confirming the approach works
</decisions_made>

<blockers>
- **Coolify API base URL unknown**: The MCP server connects somewhere but we couldn't find where. Check Coolify admin panel URL (may be on a custom port or subdomain). The server's `sentinel_custom_url` was `http://host.docker.internal:8000` which suggests Coolify runs on port 8000 internally.
- **Temporary API key**: User provided `CyAm2Uj3CuH...` for testing — needs to be deleted and replaced with a secure approach (env var in MCP config or similar).
</blockers>

<context>
Key UUIDs:
- Cascadia app: bkc8kswc4g4gck008w0kw8k4
- Boulder app: bcokg04wc0kk440gw0ooosgw
- Coolify project: n0sc0wcog8k4kkc48k0k0wo0
- Coolify server: q4okokcg8occ88w00c4kg0sw

MCP server: @masonator/coolify-mcp@2.7.3 (npx)

The provisioning automation pattern for LocalNodes-as-a-Service:
1. POST /api/v1/applications/dockercompose — create app from compose
2. PATCH /api/v1/applications/{uuid} — set docker_compose_domains + env vars
3. POST /api/v1/applications/{uuid}/envs — set DEMO_MODULE, DRUPAL_SITE_NAME, GEMINI_API_KEY
4. Deploy
</context>

<next_action>
1. Find Coolify API base URL (SSH to server, check `docker inspect` for coolify container, or check Coolify's own .env)
2. Test `docker_compose_domains` PATCH call for Cascadia
3. Redeploy and verify
4. Delete temporary API key
5. Update memory + coolify skill with findings
</next_action>
