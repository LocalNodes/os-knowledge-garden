---
phase: 09-deploy-demo-instances-to-coolify-cascadia-localnodes-xyz-boulder-localnodes-xyz
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-02T09:00:00.000Z
---

<current_state>
Both instances live and healthy. Cascadia recreated as Application (not Service).
Provisioning GitHub Action committed. Need to test it and finish 09-03 verification.
</current_state>

<completed_work>

## Session 2026-03-02 (latest)

### Coolify upgraded to v4.0.0-beta.463
- **BREAKTHROUGH:** `docker_compose_domains` PATCH now works without `docker_compose_raw`
- Format changed: must be actual JSON array, not JSON string
- Full automation flow proven: create → PATCH domains → update env vars → deploy

### Cascadia recreated as Application (not Service)
- Old Cascadia was a Service (wrong type — can't build Dockerfiles, no git integration)
- New Cascadia: `pok0ckskksok0sgsokwk8ccw` → https://cascadia.localnodes.xyz
- Created 100% via API — zero UI interaction
- SIWE domain auto-configured to cascadia.localnodes.xyz

### Services vs Applications research (definitive)
- **Applications:** Git-connected, supports build from Dockerfile, CI/CD triggers, docker_compose_domains
- **Services:** No git, compose YAML stored in DB, can't build from Dockerfiles
- **Always use Applications** for docker-compose from git repos

### Image 500 fix
- Root cause: chown runs before install, demo content creates files owned by root
- Fix: added second chown after install/content-loading block (commit 0802c7c)
- Immediate fix: restart runs chown on existing files via existing-install path

### Provisioning workflow prototype (commit d0fd17f)
- `.github/workflows/provision-instance.yml` — workflow_dispatch
- Inputs: subdomain, site_name, demo_module
- Flow: validate subdomain → check availability → create app → set domain → configure env vars → deploy → health check
- **Needs testing:** requires COOLIFY_API_TOKEN and GEMINI_API_KEY GitHub secrets

## Both Instances Status
- **Cascadia:** `pok0ckskksok0sgsokwk8ccw` — running:healthy, https://cascadia.localnodes.xyz ✓
- **Boulder:** `bcokg04wc0kk440gw0ooosgw` — running:healthy, https://boulder.localnodes.xyz ✓

</completed_work>

<remaining_work>

## Priority 1: Test provisioning workflow
- Add GitHub secrets: `COOLIFY_API_TOKEN`, `GEMINI_API_KEY`
- Run workflow_dispatch with a test subdomain (e.g., "test-provision")
- Verify the full flow works end-to-end
- Fix any API endpoint issues (env var PATCH endpoint needs verification)
- Delete test instance after verification

## Priority 2: Execute 09-03 verification plan
- Full verification checklist for both Cascadia and Boulder
- HTTP, SSL, content, chatbot, search, container health
- Human verification checkpoint (browser testing)

## Priority 3: Enhance provisioning for production use
- Add teardown workflow (delete instance + volumes)
- Consider Cloudflare Worker wrapper for non-GitHub user self-service
- Add Wildcard DNS documentation
- Consider adding `demo_module: custom` option for blank instances

</remaining_work>

<decisions_made>
- Applications (not Services) for all docker-compose deployments
- Two-step API (create + PATCH docker_compose_domains) is the standard provisioning flow
- GitHub Actions workflow_dispatch for LocalNodes-as-a-Service provisioning
- Coolify beta.463 fixes the critical base64 PATCH bug — API-only provisioning is reliable
</decisions_made>

<context>
Key UUIDs:
- Cascadia app: pok0ckskksok0sgsokwk8ccw (Application, RUNNING)
- Boulder app: bcokg04wc0kk440gw0ooosgw (Application, RUNNING)
- Coolify project: n0sc0wcog8k4kkc48k0k0wo0
- Coolify server: q4okokcg8occ88w00c4kg0sw
- Environment: production

API: https://coolify.localnodes.xyz/api/v1/
API key: AR8J3RWRy5xGoKe1aS8zHd9QfG6f71u2CR3sJM4kb6501931
Coolify version: v4.0.0-beta.463

Workflows:
- .github/workflows/build-image.yml (Docker build, runs on push to main)
- .github/workflows/provision-instance.yml (Instance provisioning, workflow_dispatch)

GitHub secrets needed: COOLIFY_API_TOKEN, GEMINI_API_KEY
</context>

<next_action>
1. Add COOLIFY_API_TOKEN and GEMINI_API_KEY as GitHub repo secrets
2. Test provision-instance workflow with a test subdomain
3. Run 09-03 verification on both live instances
</next_action>
