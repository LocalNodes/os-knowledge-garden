---
phase: 09-deploy-demo-instances-to-coolify-cascadia-localnodes-xyz-boulder-localnodes-xyz
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-03-01T10:12:00.000Z
---

<current_state>
Phase 09, Plan 02 in progress. Three bug fixes committed and pushed. CI building commit 51f14f1. After CI completes, redeploy both Coolify apps to pick up all fixes.

Domain routing already fixed manually by user.
</current_state>

<completed_work>

## Plan 09-01: Docker/CI Infrastructure (COMPLETE)
- All 3 tasks done, SUMMARY.md created

## Plan 09-02: Coolify App Creation (IN PROGRESS)
- Coolify apps created: Cascadia u0oksog0wgsosw8c48c8sog0, Boulder w8sss4k488okss4kwc000sss
- All env vars set, GHCR package public, domains set manually
- Three bug fixes committed:
  1. **settings.php ParseError** (commit 88d0d7d): Removed `<?php` from entrypoint heredoc -- source settings.php is 903 lines and already open in PHP context, appending `<?php` caused ParseError at line 904
  2. **Solr core load failure** (commit 88d0d7d): Added `SOLR_MODULES: analysis-extras` to docker-compose.yaml -- configset uses `solr.ICUCollationField` which requires that module
  3. **Solr healthcheck** (commit 88d0d7d): Changed from `/solr/` to `/solr/drupal/admin/ping` -- ensures core is loaded before depends_on passes
  4. **Docker image cache** (commit 51f14f1): Added `pull_policy: always` to opensocial service -- without this, Docker on server uses cached old image and fixes never reach running container

</completed_work>

<remaining_work>

## Immediate: Redeploy after CI completes
- CI is building commit 51f14f1 (push triggered ~10:10 UTC)
- After CI passes, redeploy both apps:
  - Cascadia: `mcp__coolify__deploy` with uuid `u0oksog0wgsosw8c48c8sog0`, force=true
  - Boulder: `mcp__coolify__deploy` with uuid `w8sss4k488okss4kwc000sss`, force=true
- Check container logs to verify settings.php error is gone and Solr core loads

## After containers are healthy
- 09-02 Task 3: checkpoint human-verify (test domains, check site loads)
- Create 09-02-SUMMARY.md (DO NOT include API keys)
- 09-03: Full verification (domain access, SSL, demo content, AI features, health)
- Phase verifier

</remaining_work>

<decisions_made>

- Removed `<?php` from entrypoint heredoc -- source settings.php (903 lines) already has `<?php` open, never closes with `?>`
- Added SOLR_MODULES=analysis-extras -- ICUCollationField requires analysis-extras module
- Changed Solr healthcheck to /solr/drupal/admin/ping -- verifies core loaded
- Added pull_policy: always -- ensures Docker always pulls latest image instead of using server cache
- Domain routing fixed manually by user in Coolify UI

</decisions_made>

<blockers>
- None currently -- all known issues have fixes committed, awaiting CI + redeploy
</blockers>

<context>
Key UUIDs:
- Cascadia app: u0oksog0wgsosw8c48c8sog0
- Boulder app: w8sss4k488okss4kwc000sss
- Coolify project: n0sc0wcog8k4kkc48k0k0wo0
- Coolify server: q4okokcg8occ88w00c4kg0sw

The root cause chain was:
1. entrypoint.sh heredoc included `<?php` but source settings.php already has it open (903 lines, never closes) -> ParseError at line 904
2. Solr configset needs analysis-extras module for ICUCollationField -> core fails to load
3. Solr healthcheck only checked admin UI, not core -> depends_on passed despite core failure
4. Docker on Coolify server cached old image -> fix didn't reach running container

All four fixes are in commits 88d0d7d and 51f14f1. CI for 51f14f1 should complete ~10:12 UTC.
</context>

<next_action>
1. Wait for CI to complete on commit 51f14f1 (check: `gh run list --limit 1`)
2. Redeploy both apps via Coolify API with force=true
3. Check container logs -- settings.php error should be gone, Solr core should load
4. If healthy, proceed to 09-02 verification and 09-03
</next_action>
