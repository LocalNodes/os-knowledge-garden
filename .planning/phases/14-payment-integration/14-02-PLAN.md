---
phase: 14-payment-integration
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - localnodes-onboarding/server/api/stripe-webhook.post.ts
  - localnodes-onboarding/app/pages/success.vue
  - localnodes-onboarding/app/pages/cancel.vue
  - localnodes-onboarding/tests/unit/stripe-webhook.test.ts
autonomous: false
requirements: [PAY-01, PAY-02]
user_setup:
  - service: stripe
    why: "Webhook endpoint and receipt email configuration"
    env_vars:
      - name: NUXT_STRIPE_WEBHOOK_SECRET
        source: "For local dev: printed by `stripe listen` CLI command (whsec_xxx). For production: Stripe Dashboard -> Developers -> Webhooks -> endpoint signing secret"
    dashboard_config:
      - task: "Enable receipt emails"
        location: "Stripe Dashboard -> Settings -> Customer emails -> Toggle 'Successful payments' ON"
      - task: "Create webhook endpoint (production)"
        location: "Stripe Dashboard -> Developers -> Webhooks -> + Add endpoint -> URL: https://localnodes.xyz/api/stripe-webhook -> Events: checkout.session.completed"

must_haves:
  truths:
    - "Stripe webhook handler receives checkout.session.completed events and verifies the signature using the raw request body"
    - "User sees a success page after completing payment on Stripe"
    - "User sees a cancel page with a 'Try Again' link if they abandon Stripe Checkout"
    - "Stripe sends a receipt email to the user after successful payment (Dashboard config)"
  artifacts:
    - path: "localnodes-onboarding/server/api/stripe-webhook.post.ts"
      provides: "Webhook handler with signature verification"
      contains: "constructEvent"
    - path: "localnodes-onboarding/app/pages/success.vue"
      provides: "Post-payment success page"
      contains: "Payment successful"
    - path: "localnodes-onboarding/app/pages/cancel.vue"
      provides: "Payment cancelled page"
      contains: "Payment cancelled"
    - path: "localnodes-onboarding/tests/unit/stripe-webhook.test.ts"
      provides: "Unit tests for webhook signature verification and event routing"
      min_lines: 30
  key_links:
    - from: "localnodes-onboarding/server/api/stripe-webhook.post.ts"
      to: "localnodes-onboarding/server/utils/stripe.ts"
      via: "useStripe() for constructEvent"
      pattern: "useStripe\\(\\)"
    - from: "Stripe Dashboard"
      to: "/api/stripe-webhook"
      via: "Webhook POST with stripe-signature header"
      pattern: "stripe-signature"
---

<objective>
Create the Stripe webhook handler with signature verification, build success and cancel pages for the post-checkout redirect flow, and verify Stripe Dashboard receipt email configuration.

Purpose: Completes the payment loop -- the webhook proves payment happened (required for Phase 15 provisioning trigger), and the success/cancel pages give the user immediate feedback after interacting with Stripe Checkout.

Output: Working webhook endpoint (logs events for now, Phase 15 adds provisioning trigger), success and cancel Vue pages, and confirmed receipt email configuration.
</objective>

<execution_context>
@/Users/proofoftom/.claude/get-shit-done/workflows/execute-plan.md
@/Users/proofoftom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-payment-integration/14-RESEARCH.md

# Plan 01 output (Stripe SDK installed, config set, singleton created)
@.planning/phases/14-payment-integration/14-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: Stripe client singleton (will exist after 14-01 completes) -->
From localnodes-onboarding/server/utils/stripe.ts:
```typescript
// Auto-imported by Nitro in all server/ files
export function useStripe(): Stripe
```

From localnodes-onboarding/nuxt.config.ts (after Plan 01):
```typescript
runtimeConfig: {
  coolifyApiUrl: 'https://coolify.localnodes.xyz/api/v1',
  coolifyApiToken: '',
  stripeSecretKey: '',
  stripeWebhookSecret: '',
  stripePriceId: '',
}
routeRules: {
  '/': { prerender: true },
  '/onboarding': { ssr: true },
  '/success': { ssr: true },
  '/cancel': { prerender: true }
}
```

<!-- Existing Nuxt UI components available for pages -->
Available from @nuxt/ui: UPageSection, UButton, UIcon (Lucide icons via i-lucide-* prefix)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Stripe webhook handler with signature verification and tests</name>
  <files>
    localnodes-onboarding/server/api/stripe-webhook.post.ts,
    localnodes-onboarding/tests/unit/stripe-webhook.test.ts
  </files>
  <behavior>
    - Test: Returns 400 when stripe-signature header is missing
    - Test: Returns 400 when request body is empty
    - Test: Returns 400 when signature verification fails (invalid signature)
    - Test: Returns {received: true} for valid checkout.session.completed event
    - Test: Logs session metadata (sessionId, customerEmail, metadata, subscriptionId) for checkout.session.completed
    - Test: Returns {received: true} for unhandled event types (no error, just log)
  </behavior>
  <action>
    1. Create `tests/unit/stripe-webhook.test.ts` -- TDD RED phase:
       - Extract the webhook handler's core logic into testable functions:
         - `validateWebhookHeaders(signature: string | undefined)` -- throws if missing
         - `parseWebhookEvent(rawBody: string, signature: string, secret: string, stripe: Stripe)` -- calls constructEvent, throws on failure
         - `handleCheckoutCompleted(session: Stripe.Checkout.Session)` -- logs and returns metadata
       - Mock the Stripe SDK's `webhooks.constructEvent()` method
       - Test each validation branch and the checkout.session.completed handler
       - Follow the existing test pattern from check-subdomain.test.ts (test extracted pure logic)

    2. Create `server/api/stripe-webhook.post.ts` -- TDD GREEN phase:
       - Get `stripe-signature` header via `getHeader(event, 'stripe-signature')`
       - If missing: `throw createError({ status: 400, statusText: 'Missing Stripe signature' })`
       - Get raw body via `readRawBody(event)` (NOT readBody -- destroys signature per Research pitfall 1)
       - If empty: `throw createError({ status: 400, statusText: 'Missing request body' })`
       - Call `stripe.webhooks.constructEvent(rawBody, signature, config.stripeWebhookSecret)`
       - Wrap in try/catch: on failure, log error and throw 400 with 'Invalid webhook signature'
       - Switch on `stripeEvent.type`:
         - `checkout.session.completed`: Log sessionId, customer_email, metadata, subscription ID. Add comment: `// Phase 15 will add: trigger provisioning via GitHub Actions`
         - default: Log unhandled event type
       - Return `{ received: true }` immediately (within Stripe's 20-second timeout)

    Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npx vitest run tests/unit/stripe-webhook.test.ts</automated>
  </verify>
  <done>
    - server/api/stripe-webhook.post.ts verifies Stripe webhook signatures using readRawBody
    - checkout.session.completed events are logged with session metadata
    - Returns {received: true} for all valid events
    - Returns 400 for missing signature, missing body, or invalid signature
    - All webhook tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create success and cancel pages for post-checkout redirect</name>
  <files>
    localnodes-onboarding/app/pages/success.vue,
    localnodes-onboarding/app/pages/cancel.vue
  </files>
  <action>
    1. Create `app/pages/success.vue`:
       - SEO meta: title "Payment Successful - LocalNodes", description about garden being set up
       - Read `session_id` from `useRoute().query.session_id` (for future Phase 16 status tracking)
       - Centered layout using UPageSection with max-w-md
       - Green check-circle icon (UIcon with i-lucide-check-circle, text-green-500, large size)
       - Heading: "Payment successful!"
       - Body text: "Thank you for your payment. Your knowledge garden is being prepared. You'll receive a welcome email when it's ready."
       - Comment placeholder: `<!-- Phase 16 will add: progress/status UI here -->`
       - UButton "Back to Home" linking to "/" with outline variant

    2. Create `app/pages/cancel.vue`:
       - SEO meta: title "Payment Cancelled - LocalNodes", description that no charges were made
       - Centered layout using UPageSection with max-w-md
       - Arrow-left icon (UIcon with i-lucide-arrow-left, text muted color, large size)
       - Heading: "Payment cancelled"
       - Body text: "No charges were made. You can try again whenever you're ready."
       - UButton "Try Again" linking to "/onboarding" with primary color

    Both pages should match the existing app's dark theme and use Nuxt UI components consistently with the landing page and onboarding page.
  </action>
  <verify>
    <automated>cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npx vitest run</automated>
  </verify>
  <done>
    - /success page shows payment confirmation with green check icon and session_id captured from query
    - /cancel page shows cancellation message with "Try Again" link back to /onboarding
    - Both pages have SEO meta tags
    - All existing tests still pass (no regressions)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end Stripe payment flow and Dashboard receipt configuration</name>
  <files>none</files>
  <action>Human verification of the complete payment flow in test mode. All code was completed in Tasks 1-2 and Plan 14-01. User tests the full checkout redirect, webhook delivery, success/cancel pages, and Stripe Dashboard receipt email toggle.</action>
  <what-built>
    Complete Stripe payment integration: form submits to checkout endpoint, webhook handler receives events, success/cancel pages handle post-checkout redirect.
  </what-built>
  <how-to-verify>
    1. **Stripe Dashboard setup** (one-time, required before testing):
       - Log into https://dashboard.stripe.com (test mode)
       - Create Product: "LocalNodes Knowledge Garden" at $29/month
       - Copy the Price ID (price_xxx) and set as NUXT_STRIPE_PRICE_ID in .env
       - Copy the Secret Key (sk_test_xxx) and set as NUXT_STRIPE_SECRET_KEY in .env
       - Go to Settings > Customer emails > Toggle "Successful payments" ON (PAY-02)

    2. **Local webhook testing** (requires Stripe CLI):
       - Run `stripe listen --forward-to localhost:3000/api/stripe-webhook --events checkout.session.completed`
       - Copy the webhook signing secret (whsec_xxx) and set as NUXT_STRIPE_WEBHOOK_SECRET in .env

    3. **Test the full flow**:
       - Start dev server: `cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npm run dev`
       - Visit http://localhost:3000/onboarding
       - Enter a community name and email, wait for subdomain to show "available"
       - Click "Continue to Payment" -- verify button shows loading state
       - Confirm redirect to Stripe Checkout page (stripe.com domain)
       - Use test card 4242 4242 4242 4242 (any future date, any CVC)
       - After payment: verify redirect to /success page with green check and confirmation message
       - Check Stripe CLI terminal: verify webhook received and logged session metadata
       - Test cancel flow: go through checkout again, click back/cancel on Stripe -- verify /cancel page appears

    4. **Verify receipt email (PAY-02)**:
       - In Stripe Dashboard > Payments, find the test payment
       - Click "Send receipt" to verify the email template works (test mode does not auto-send)

    Expected: Complete payment flow works end-to-end in test mode.
  </how-to-verify>
  <verify>Manual end-to-end verification by user</verify>
  <done>User confirms complete payment flow works: form submit, Stripe redirect, payment, success page, webhook delivery, and receipt email configuration</done>
  <resume-signal>Type "approved" or describe any issues with the payment flow</resume-signal>
</task>

</tasks>

<verification>
1. `cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npx vitest run` -- all tests pass (existing + webhook)
2. `grep "constructEvent" localnodes-onboarding/server/api/stripe-webhook.post.ts` -- signature verification present
3. `grep "readRawBody" localnodes-onboarding/server/api/stripe-webhook.post.ts` -- uses raw body (not parsed JSON)
4. Success and cancel pages exist and render correctly at /success and /cancel
</verification>

<success_criteria>
- Webhook handler verifies Stripe signatures using readRawBody and constructEvent (not readBody)
- checkout.session.completed events are logged with session metadata (ready for Phase 15 to add provisioning trigger)
- Success page shows payment confirmation and captures session_id from query string
- Cancel page shows "no charges made" message with link back to onboarding
- Stripe Dashboard receipt emails enabled (manual verification)
- All unit tests pass including webhook handler tests
</success_criteria>

<output>
After completion, create `.planning/phases/14-payment-integration/14-02-SUMMARY.md`
</output>
