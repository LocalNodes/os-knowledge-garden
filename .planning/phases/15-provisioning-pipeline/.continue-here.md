---
phase: 15-provisioning-pipeline
task: 2 (checkpoint)
total_tasks: 2
status: in_progress
last_updated: 2026-03-04T13:51:21Z
---

<current_state>
Phase 15, Plan 15-02, Task 2 (E2E checkpoint) — nearly complete.

The entrypoint-based provisioning is **E2E verified and working**. User creation, role assignment, login URL, welcome email (via Resend), and status callbacks all run from the Docker entrypoint during fresh install. No more SSH dependency.

Also fixed Drupal's outbound email (sendmail → Resend SMTP) as a bonus fix discovered during testing.

What remains: update STATE.md, complete the 15-02 checkpoint, and handle existing instances.
</current_state>

<completed_work>

- Plan 15-01: All done (Redis state, GitHub dispatch, idempotency, status/callback endpoints)
- Plan 15-02, Task 1: Workflow extended with new inputs, callbacks — committed
- Plan 15-02, Task 2 (E2E):
  - Moved user creation from SSH to Docker entrypoint (de4374e)
  - Fixed Drupal email: sendmail → Resend SMTP transport (6ed8166)
  - Fixed Coolify env var creation: POST fallback for new vars (bece6db, 9d334a1)
  - **E2E test passed**: user created, sitemanager role, login URL, welcome email sent, zero sendmail errors
  - Test instance cleaned up

</completed_work>

<remaining_work>

- Complete 15-02 checkpoint (type "approved" in execute-phase context)
- Phase execution will then: create SUMMARY, run verification, update roadmap
- Existing instances (Cascadia, Boulder, Portland) need RESEND_API_KEY env var + fresh deploy to fix their sendmail errors
- Proceed to Phase 16 or 17 after 15 completes

</remaining_work>

<decisions_made>

- Move user creation from GitHub Actions SSH to Docker entrypoint (SSH port 22 blocked from GH runners)
- Entrypoint handles: user creation, role assignment, login URL generation, status callbacks, welcome email via Resend API
- Pass organizer details + secrets as Coolify env vars (ORGANIZER_EMAIL, ORGANIZER_NAME, RESEND_API_KEY, etc.)
- Coolify API: PATCH only updates existing env vars, POST creates new ones — workflow now tries PATCH then falls back to POST
- POST body must NOT include is_build_time (causes 422)
- Drupal outbound email: switched symfony_mailer from sendmail transport to SMTP via Resend (smtp.resend.com:465), API key injected via settings.php $config override

</decisions_made>

<blockers>

None — E2E is passing. Existing instances just need RESEND_API_KEY set (user has the key value).
</blockers>

<context>
The phase execution orchestrator was mid-wave-2 when we paused last session. This session we implemented the entrypoint approach, discovered and fixed two additional issues (env var creation API, Drupal sendmail), and got E2E passing. The checkpoint needs to be formally completed via /gsd:execute-phase to trigger SUMMARY creation and phase transition.

4 commits on main since last session:
- de4374e refactor(15-02): move user creation from SSH to Docker entrypoint
- 6ed8166 fix: configure Resend SMTP for Drupal outbound email
- bece6db fix: use POST fallback for new Coolify env vars
- 9d334a1 fix: remove is_build_time from env var POST (causes 422)
</context>

<next_action>
Resume with `/gsd:resume-work`. Next step is to complete the 15-02 E2E checkpoint, which triggers SUMMARY creation and phase verification. Then set RESEND_API_KEY on existing instances and proceed to Phase 16.
</next_action>
