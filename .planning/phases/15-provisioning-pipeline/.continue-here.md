---
phase: 15-provisioning-pipeline
task: 2 (checkpoint)
total_tasks: 2
status: in_progress
last_updated: 2026-03-04T12:20:08.398Z
---

<current_state>
Phase 15 has 2 plans across 2 waves. Both plans' code tasks are COMPLETE and committed.
We are at the **checkpoint (human-verify)** in Plan 15-02, Task 2 — verifying the end-to-end provisioning pipeline.

The first E2E test was partially successful: a site was provisioned at https://my-test-community.localnodes.xyz/ but the GitHub Actions workflow failed at the "Setup SSH" step due to a malformed SSH_PRIVATE_KEY secret. The key was re-set properly from the server. A re-run failed because the subdomain already exists (idempotency working correctly). Need to test with a fresh subdomain or delete the test instance first.
</current_state>

<completed_work>

- Plan 15-01, Task 1: Redis/GitHub utils, triggerProvisioning, idempotency tests - Done (commit e47a3bb, b286e89)
- Plan 15-01, Task 2: Provision status polling and callback endpoints - Done (commit 1e59e55, 0deaf15)
- Plan 15-01 SUMMARY: Done (commit b736d2d)
- Plan 15-02, Task 1: Extended provisioning workflow with inputs, user creation, welcome email, callbacks - Done (commit c727bff)
- Plan 15-02, Task 2: E2E checkpoint - IN PROGRESS (awaiting re-test)

Infrastructure setup completed this session:
- Created GitHub repo LocalNodes/localnodes-onboarding and pushed code
- Created Vercel project under "localnodes" team, connected to GitHub
- Set all 9 Vercel env vars (had to fix corrupted values from trailing newlines)
- Moved localnodes.xyz domain from old project to new onboarding app
- Set PROVISION_CALLBACK_SECRET + SSH_PRIVATE_KEY + RESEND_API_KEY as GitHub secrets on os-knowledge-garden
- Fixed SSH_PRIVATE_KEY formatting by re-setting from server with `gh secret set < keyfile`
- Pushed fresh3 to origin (workflow changes live on GitHub)
</completed_work>

<remaining_work>

- Plan 15-02, Task 2 (checkpoint): Re-test E2E with a fresh subdomain. Verify: site provisions, admin user created, welcome email received, login link works. Then type "approved".
- After checkpoint approved: executor agent creates 15-02-SUMMARY.md, updates STATE.md and ROADMAP.md
- Phase verification (gsd-verifier agent)
- Phase completion (roadmap update, state advance)
</remaining_work>

<decisions_made>

- Vercel team: user renamed "relieth" to "localnodes", used that scope
- GitHub repo: LocalNodes/localnodes-onboarding (public)
- Domain: localnodes.xyz moved from tommy-coxs-projects/localnodes-landing-claude to localnodes/localnodes-onboarding
- SSH key: ed25519 key generated on server at /root/.ssh/github-actions, public key uploaded to GitHub via gh cli on server
- Callback secret: generated with openssl rand -hex 32, stored in both Vercel (NUXT_PROVISION_CALLBACK_SECRET) and GitHub (PROVISION_CALLBACK_SECRET)
- Demo content issue noted: new instances get localnodes_demo content by default — should probably be blank for paid instances (future fix)
</decisions_made>

<blockers>

- SSH key was corrupted in GitHub secret (newlines lost during paste) — FIXED by re-setting from server with `gh secret set SSH_PRIVATE_KEY < /root/.ssh/github-actions`
- Vercel env vars were corrupted (trailing `\n"`) when set via `source .env && vercel env add <<< "$VAR"` — FIXED by using `printf '%s' "$value" | vercel env add`
- Re-run of workflow failed because subdomain "my-test-community" already exists — need fresh subdomain or delete test instance first
</blockers>

<context>
The phase execution orchestrator is mid-wave-2. Plan 15-02's executor returned a checkpoint for Task 2 (human-verify). When resuming, the user needs to:
1. Either delete the my-test-community test instance or use a new subdomain
2. Test the full E2E flow: Stripe checkout → webhook → GitHub Actions → Coolify → user creation → welcome email
3. Type "approved" to complete the checkpoint
4. The phase execution will then proceed to: SUMMARY creation, verification, roadmap update
</context>

<next_action>
Resume with `/gsd:resume-work`. Delete test instance (my-test-community on Coolify) or use a new subdomain. Complete E2E test and approve checkpoint to finish Phase 15.
</next_action>
