---
phase: 15-provisioning-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - localnodes-onboarding/server/utils/redis.ts
  - localnodes-onboarding/server/utils/github.ts
  - localnodes-onboarding/server/api/stripe-webhook.post.ts
  - localnodes-onboarding/server/api/provision-status.get.ts
  - localnodes-onboarding/server/api/provision-callback.post.ts
  - localnodes-onboarding/nuxt.config.ts
  - localnodes-onboarding/.env.example
  - localnodes-onboarding/tests/unit/provision-trigger.test.ts
  - localnodes-onboarding/tests/unit/idempotency.test.ts
  - localnodes-onboarding/tests/unit/provision-status.test.ts
autonomous: true
requirements:
  - PROV-01
  - PROV-04

user_setup:
  - service: upstash-redis
    why: "State bridge between Stripe webhooks, GitHub Actions, and frontend status polling"
    env_vars:
      - name: NUXT_UPSTASH_REDIS_URL
        source: "Upstash Console -> Create Database -> REST URL"
      - name: NUXT_UPSTASH_REDIS_TOKEN
        source: "Upstash Console -> Create Database -> REST Token"
    dashboard_config:
      - task: "Create a new Redis database"
        location: "Upstash Console -> Create Database (choose nearest region, free tier works)"
  - service: github-pat
    why: "Dispatch provisioning workflow from webhook handler"
    env_vars:
      - name: NUXT_GITHUB_TOKEN
        source: "GitHub -> Settings -> Developer Settings -> Fine-grained PATs -> Generate new token"
    dashboard_config:
      - task: "Create fine-grained PAT with actions:write + metadata:read on LocalNodes/os-knowledge-garden"
        location: "GitHub Settings -> Developer Settings -> Personal Access Tokens -> Fine-grained tokens"
  - service: provision-callback
    why: "Shared secret for GitHub Actions to authenticate callbacks to the Nitro server"
    env_vars:
      - name: NUXT_PROVISION_CALLBACK_SECRET
        source: "Generate with: openssl rand -hex 32"

must_haves:
  truths:
    - "Stripe checkout.session.completed webhook triggers GitHub Actions provisioning workflow automatically"
    - "Duplicate webhook deliveries for the same Stripe session do not trigger duplicate provisioning"
    - "Frontend can poll provisioning status by session_id and receive structured state"
    - "GitHub Actions workflow can POST status updates back to the Nitro server"
  artifacts:
    - path: "localnodes-onboarding/server/utils/redis.ts"
      provides: "Upstash Redis singleton + ProvisioningState type + get/set helpers"
      exports: ["useRedis", "ProvisioningState", "setProvisioningState", "getProvisioningState"]
    - path: "localnodes-onboarding/server/utils/github.ts"
      provides: "GitHub API helper for workflow dispatch with return_run_details"
      exports: ["dispatchProvisioningWorkflow"]
    - path: "localnodes-onboarding/server/api/stripe-webhook.post.ts"
      provides: "Modified webhook handler that triggers provisioning on checkout.session.completed"
      contains: "triggerProvisioning"
    - path: "localnodes-onboarding/server/api/provision-status.get.ts"
      provides: "GET endpoint returning provisioning state from Redis"
      exports: ["default"]
    - path: "localnodes-onboarding/server/api/provision-callback.post.ts"
      provides: "POST endpoint for GitHub Actions to update provisioning state"
      exports: ["default"]
  key_links:
    - from: "localnodes-onboarding/server/api/stripe-webhook.post.ts"
      to: "localnodes-onboarding/server/utils/redis.ts"
      via: "triggerProvisioning calls setProvisioningState + redis.set(nx)"
      pattern: "setProvisioningState|redis\\.set"
    - from: "localnodes-onboarding/server/api/stripe-webhook.post.ts"
      to: "localnodes-onboarding/server/utils/github.ts"
      via: "triggerProvisioning calls dispatchProvisioningWorkflow"
      pattern: "dispatchProvisioningWorkflow"
    - from: "localnodes-onboarding/server/api/provision-status.get.ts"
      to: "localnodes-onboarding/server/utils/redis.ts"
      via: "getProvisioningState reads Redis hash"
      pattern: "getProvisioningState"
    - from: "localnodes-onboarding/server/api/provision-callback.post.ts"
      to: "localnodes-onboarding/server/utils/redis.ts"
      via: "setProvisioningState writes Redis hash"
      pattern: "setProvisioningState"
---

<objective>
Build the Nuxt server-side provisioning infrastructure: Redis state tracking, GitHub Actions dispatch, idempotency guard, status polling endpoint, and callback endpoint for workflow progress updates.

Purpose: This is the server-side glue that connects Stripe payment confirmation to GitHub Actions provisioning. The webhook handler must respond fast (under Stripe's 20s timeout), dispatch provisioning asynchronously, and track state in Redis for frontend polling.

Output: Five server files (2 utils, 3 API routes) with full test coverage for the dispatch, idempotency, and status logic.
</objective>

<execution_context>
@/Users/proofoftom/.claude/get-shit-done/workflows/execute-plan.md
@/Users/proofoftom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-provisioning-pipeline/15-RESEARCH.md
@.planning/phases/14-payment-integration/14-01-SUMMARY.md
@.planning/phases/14-payment-integration/14-02-SUMMARY.md

<interfaces>
<!-- Existing code the executor needs. Extracted from codebase. -->

From localnodes-onboarding/server/utils/stripe.ts:
```typescript
import Stripe from 'stripe'
let _stripe: Stripe | null = null
export function useStripe(): Stripe {
  if (!_stripe) {
    const config = useRuntimeConfig()
    if (!config.stripeSecretKey) {
      throw new Error('NUXT_STRIPE_SECRET_KEY environment variable is not set')
    }
    _stripe = new Stripe(config.stripeSecretKey)
  }
  return _stripe
}
```

From localnodes-onboarding/server/api/stripe-webhook.post.ts (current state):
```typescript
export default defineEventHandler(async (event) => {
  const signature = getHeader(event, 'stripe-signature')
  // ... signature verification ...
  switch (stripeEvent.type) {
    case 'checkout.session.completed': {
      const session = stripeEvent.data.object
      console.log('Checkout session completed:', { sessionId: session.id, customerEmail: session.customer_email, metadata: session.metadata, subscriptionId: session.subscription })
      // Phase 15 will add: trigger provisioning via GitHub Actions
      break
    }
  }
  return { received: true }
})
```

From localnodes-onboarding/nuxt.config.ts (current runtimeConfig):
```typescript
runtimeConfig: {
  coolifyApiUrl: 'https://coolify.localnodes.xyz/api/v1',
  coolifyApiToken: '',
  stripeSecretKey: '',
  stripeWebhookSecret: '',
  stripePriceId: '',
}
```

From localnodes-onboarding/server/api/create-checkout.post.ts (metadata shape):
```typescript
metadata: {
  communityName: body.communityName,
  subdomain: body.subdomain,
  email: body.email
}
```

From localnodes-onboarding/vitest.config.ts:
```typescript
import { defineConfig } from 'vitest/config'
import { resolve } from 'path'
export default defineConfig({
  test: { environment: 'node', include: ['tests/**/*.test.ts'], globals: true },
  resolve: { alias: { '~': resolve(__dirname, 'app'), '#app': resolve(__dirname, '.nuxt/imports') } }
})
```

Existing test pattern (from tests/unit/check-subdomain.test.ts, tests/unit/stripe-webhook.test.ts):
- Pure function extraction from server routes for testability
- Mock external dependencies (Stripe, Coolify API)
- vi.mock/vi.fn for dependency injection
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redis/GitHub utils, triggerProvisioning function, and idempotency tests</name>
  <files>
    localnodes-onboarding/server/utils/redis.ts,
    localnodes-onboarding/server/utils/github.ts,
    localnodes-onboarding/server/api/stripe-webhook.post.ts,
    localnodes-onboarding/nuxt.config.ts,
    localnodes-onboarding/.env.example,
    localnodes-onboarding/tests/unit/provision-trigger.test.ts,
    localnodes-onboarding/tests/unit/idempotency.test.ts
  </files>
  <behavior>
    provision-trigger.test.ts:
    - Test: triggerProvisioning sets Redis lock key via set(nx:true) and returns alreadyProcessing=false on first call
    - Test: triggerProvisioning sets initial provisioning state with status='triggered' including subdomain, email, siteName, stripeSessionId, startedAt
    - Test: triggerProvisioning calls dispatchProvisioningWorkflow with correct params (subdomain, siteName, email, stripeSessionId)
    - Test: triggerProvisioning updates state to status='provisioning' with githubRunId and githubRunUrl after dispatch
    - Test: triggerProvisioning returns alreadyProcessing=true when lock already exists (second call for same session)

    idempotency.test.ts:
    - Test: First call with session_id acquires lock and returns alreadyProcessing=false
    - Test: Second call with same session_id returns alreadyProcessing=true without dispatching workflow
    - Test: Two different session_ids both acquire their own locks independently
    - Test: Lock uses 1-hour TTL (ex: 3600)
  </behavior>
  <action>
    All work is in the localnodes-onboarding/ directory.

    1. Install dependencies:
       ```bash
       cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding
       npm install @upstash/redis
       ```
       NOTE: Do NOT install resend -- welcome email is sent from GitHub Actions via curl, not from Nuxt.

    2. Create `server/utils/redis.ts`:
       - Singleton pattern matching existing `server/utils/stripe.ts`
       - `useRedis()` function creating `Redis` instance from `config.upstashRedisUrl` and `config.upstashRedisToken`
       - Export `ProvisioningState` interface with fields: status (union type: 'triggered' | 'provisioning' | 'installing' | 'creating_user' | 'sending_email' | 'complete' | 'failed'), subdomain, email, siteName, stripeSessionId, githubRunId?, githubRunUrl?, siteUrl?, loginUrl?, error?, startedAt, updatedAt
       - Export `setProvisioningState(sessionId, state)` -- uses `hset` on `provision:{sessionId}` key, always sets `updatedAt`, sets 24h TTL via `expire`
       - Export `getProvisioningState(sessionId)` -- uses `hgetall` on `provision:{sessionId}` key, returns null if empty

    3. Create `server/utils/github.ts`:
       - Export `dispatchProvisioningWorkflow(params)` accepting `{ subdomain, siteName, email, stripeSessionId }`
       - POST to `https://api.github.com/repos/${config.githubRepo}/actions/workflows/provision-instance.yml/dispatches`
       - Use `return_run_details: true` in request body (returns 200 with `{ workflow_run_id, html_url }`)
       - Headers: `Authorization: Bearer ${config.githubToken}`, `Accept: application/vnd.github+json`, `X-GitHub-Api-Version: 2022-11-28`
       - Inputs: subdomain, site_name (from siteName), email, stripe_session_id (from stripeSessionId), demo_module: 'localnodes_demo', callback_url: 'https://localnodes.xyz'
       - Return `{ runId: data.workflow_run_id, runUrl: data.html_url }`
       - Throw descriptive error on non-ok response

    4. Export `triggerProvisioning` function from webhook handler file (or keep in same file -- follow the pure function extraction pattern from Phase 14):
       - Extract as a standalone exported async function for testability
       - Accepts `{ id: string, metadata: { subdomain, communityName, email }, customer_email: string }`
       - Step 1: Redis SETNX on `provision:{id}:lock` with 1-hour TTL. If not acquired, return `{ alreadyProcessing: true }`
       - Step 2: Set initial state via `setProvisioningState` with status 'triggered'
       - Step 3: Call `dispatchProvisioningWorkflow`
       - Step 4: Update state to 'provisioning' with run details
       - Return `{ alreadyProcessing: false }`

    5. Modify `server/api/stripe-webhook.post.ts`:
       - Replace the Phase 15 comment placeholder with actual `triggerProvisioning` call
       - Pass session.id, session.metadata (cast as { subdomain, communityName, email }), session.customer_email
       - Log whether provisioning was already in progress
       - Keep the `return { received: true }` -- webhook must respond fast

    6. Update `nuxt.config.ts` runtimeConfig -- add these keys after the existing Stripe keys:
       ```
       upstashRedisUrl: '',        // NUXT_UPSTASH_REDIS_URL
       upstashRedisToken: '',      // NUXT_UPSTASH_REDIS_TOKEN
       githubToken: '',            // NUXT_GITHUB_TOKEN
       githubRepo: 'LocalNodes/os-knowledge-garden',  // NUXT_GITHUB_REPO
       provisionCallbackSecret: '', // NUXT_PROVISION_CALLBACK_SECRET
       ```

    7. Update `.env.example` with the new env vars (with comments explaining where to get each).

    8. Write tests FIRST (TDD red), then implement:
       - In `provision-trigger.test.ts`: mock `useRedis()` to return an object with `set`, `hset`, `expire`, `hgetall` methods (all vi.fn()). Mock `dispatchProvisioningWorkflow` via vi.mock on the github module. Test all behaviors listed above.
       - In `idempotency.test.ts`: same Redis mock approach. Test the lock acquisition logic specifically -- first call returns acquired, second returns null (already locked).
       - Follow the existing pure function test pattern from `stripe-webhook.test.ts` and `check-subdomain.test.ts`.
  </action>
  <verify>
    <automated>cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npx vitest run tests/unit/provision-trigger.test.ts tests/unit/idempotency.test.ts</automated>
  </verify>
  <done>
    - triggerProvisioning function is exported and testable
    - Redis SETNX idempotency guard prevents duplicate dispatches for same session
    - GitHub Actions dispatch uses return_run_details:true for reliable run ID
    - All provision-trigger and idempotency tests pass
    - nuxt.config.ts has all new runtimeConfig keys
    - .env.example documents all new env vars
    - Existing 62 tests still pass (no regressions)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provision status polling and callback endpoints with tests</name>
  <files>
    localnodes-onboarding/server/api/provision-status.get.ts,
    localnodes-onboarding/server/api/provision-callback.post.ts,
    localnodes-onboarding/tests/unit/provision-status.test.ts
  </files>
  <behavior>
    provision-status.test.ts:
    - Test: GET /api/provision-status?session_id=xxx returns status from Redis when state exists
    - Test: GET /api/provision-status?session_id=xxx returns { status: 'unknown' } when no state in Redis
    - Test: GET /api/provision-status without session_id returns 400 validation error
    - Test: GET /api/provision-status returns siteUrl and loginUrl when available in state
    - Test: GET /api/provision-status returns error field when status is 'failed'
    - Test: POST /api/provision-callback with valid secret updates Redis state
    - Test: POST /api/provision-callback with invalid/missing secret returns 401
    - Test: POST /api/provision-callback with invalid status value returns 422 validation error
  </behavior>
  <action>
    All work is in the localnodes-onboarding/ directory.

    1. Create `server/api/provision-status.get.ts`:
       - Validate query params with Valibot: `session_id` is required, non-empty string
       - Call `getProvisioningState(query.session_id)` from redis.ts
       - If null, return `{ status: 'unknown' }`
       - Otherwise return `{ status, siteUrl, loginUrl, error, startedAt }` (only non-null fields)
       - No authentication needed -- session_id is unguessable (Stripe session IDs are 66+ chars)

    2. Create `server/api/provision-callback.post.ts`:
       - Verify Authorization header matches `Bearer ${config.provisionCallbackSecret}` -- return 401 if not
       - Validate body with Valibot: session_id (required string), status (picklist of valid states), site_url (optional string), login_url (optional string), error (optional string)
       - Call `setProvisioningState` with the validated fields
       - Return `{ updated: true }`

    3. Add route rule in nuxt.config.ts for `/provision-status`: `{ ssr: true }` (it is a server route, but for documentation consistency).

    4. Write tests FIRST (TDD red), then implement:
       - Mock `useRedis()` and `useRuntimeConfig()` as in Task 1
       - Extract pure handler logic for testability: `handleProvisionStatus(sessionId)` and `handleProvisionCallback(authHeader, body, config)`
       - Test all behaviors listed above
  </action>
  <verify>
    <automated>cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npx vitest run tests/unit/provision-status.test.ts</automated>
  </verify>
  <done>
    - GET /api/provision-status returns structured state from Redis by session_id
    - POST /api/provision-callback accepts authenticated state updates from GitHub Actions
    - Callback rejects requests without valid Bearer token
    - All provision-status tests pass
    - Full test suite passes: npx vitest run (all existing + new tests, zero regressions)
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npx vitest run` -- all tests pass (existing 62 + new provision tests)
2. `grep -r "triggerProvisioning" localnodes-onboarding/server/` confirms the webhook handler calls triggerProvisioning
3. `grep -r "useRedis\|setProvisioningState\|getProvisioningState" localnodes-onboarding/server/` confirms Redis wiring
4. `grep -r "dispatchProvisioningWorkflow" localnodes-onboarding/server/` confirms GitHub dispatch wiring
5. `grep "upstashRedisUrl\|githubToken\|provisionCallbackSecret" localnodes-onboarding/nuxt.config.ts` confirms runtimeConfig
</verification>

<success_criteria>
- Stripe webhook handler automatically dispatches GitHub Actions provisioning workflow on checkout.session.completed
- Redis SETNX prevents duplicate provisioning for the same Stripe session (idempotent)
- Status endpoint returns structured provisioning state for frontend polling
- Callback endpoint accepts authenticated state updates from GitHub Actions
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/15-provisioning-pipeline/15-01-SUMMARY.md`
</output>
