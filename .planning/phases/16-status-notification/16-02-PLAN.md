---
phase: 16-status-notification
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - localnodes-onboarding/app/components/GardenAnimation.vue
  - localnodes-onboarding/app/components/ProvisioningProgress.vue
  - localnodes-onboarding/app/components/ProvisioningComplete.vue
  - localnodes-onboarding/app/pages/success.vue
  - docker/entrypoint.sh
autonomous: false
requirements: [STAT-01, STAT-02, STAT-03, STAT-04, NOTIF-01, NOTIF-02]

must_haves:
  truths:
    - "User sees a 4-stage progress indicator with named stages that advance as provisioning progresses"
    - "User sees an animated garden visualization with pulsing SVG elements during the wait"
    - "User sees a countdown timer ticking down from ~4:00"
    - "User sees a success page with site URL and Visit Your Garden button when provisioning completes"
    - "User receives a welcome email with site URL, one-time login link, and getting-started steps"
  artifacts:
    - path: "localnodes-onboarding/app/components/GardenAnimation.vue"
      provides: "Animated SVG garden visualization for provisioning wait"
      min_lines: 20
    - path: "localnodes-onboarding/app/components/ProvisioningProgress.vue"
      provides: "Multi-step progress indicator with completed/active/pending states"
      min_lines: 20
    - path: "localnodes-onboarding/app/components/ProvisioningComplete.vue"
      provides: "Success state with site URL and CTAs"
      min_lines: 15
    - path: "localnodes-onboarding/app/pages/success.vue"
      provides: "Full success page switching between waiting, complete, and failed states"
      min_lines: 40
    - path: "docker/entrypoint.sh"
      provides: "Enhanced welcome email with getting-started steps"
      contains: "Getting started"
  key_links:
    - from: "app/pages/success.vue"
      to: "app/composables/useProvisioningStatus.ts"
      via: "composable invocation with session_id from route query"
      pattern: "useProvisioningStatus"
    - from: "app/pages/success.vue"
      to: "app/components/ProvisioningProgress.vue"
      via: "stages prop from composable"
      pattern: "ProvisioningProgress.*stages"
    - from: "app/pages/success.vue"
      to: "app/components/GardenAnimation.vue"
      via: "rendered during waiting state"
      pattern: "GardenAnimation"
    - from: "app/pages/success.vue"
      to: "app/components/ProvisioningComplete.vue"
      via: "rendered when isComplete is true"
      pattern: "ProvisioningComplete.*isComplete"
    - from: "docker/entrypoint.sh"
      to: "Resend API"
      via: "curl POST with enhanced HTML body"
      pattern: "Getting started"
---

<objective>
Build the three UI components (GardenAnimation, ProvisioningProgress, ProvisioningComplete), rewrite success.vue to use them with the composable from Plan 01, and enhance the welcome email with getting-started content.

Purpose: This transforms the placeholder "Payment successful" page into a live progress dashboard that keeps organizers informed during the ~4 minute provisioning wait, and ensures the welcome email contains everything they need to get started.

Output: Complete progress UI with animation, stages, countdown, and success state; enhanced welcome email.
</objective>

<execution_context>
@/Users/proofoftom/.claude/get-shit-done/workflows/execute-plan.md
@/Users/proofoftom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-status-notification/16-RESEARCH.md
@.planning/phases/16-status-notification/16-01-SUMMARY.md

@localnodes-onboarding/app/components/HeroSection.vue
@localnodes-onboarding/app/pages/success.vue
@docker/entrypoint.sh

<interfaces>
<!-- Key types and contracts from Plan 01 composable -->

From app/composables/useProvisioningStatus.ts (created in Plan 01):
```typescript
export function useProvisioningStatus(sessionId: string | undefined): {
  status: Ref<RawStatus>
  stages: ComputedRef<Array<{ id: string; label: string; state: 'completed' | 'active' | 'pending' }>>
  currentStageIndex: ComputedRef<number>
  isComplete: ComputedRef<boolean>
  isFailed: ComputedRef<boolean>
  siteUrl: Ref<string | null>
  loginUrl: Ref<string | null>
  error: Ref<string | null>
  timeRemaining: ComputedRef<string>  // "M:SS" format
  count: Ref<number>                   // raw seconds remaining
  pause: () => void
}
```

From app/components/HeroSection.vue (animation pattern):
- v-motion directive for mount animations: `:initial="{ opacity: 0, y: 20 }" :enter="{ opacity: 1, y: 0 }"`
- CSS keyframes via Tailwind: `animate-[pulse_6s_ease-in-out_infinite]`, `animate-[spin_20s_linear_infinite]`
- SVG geometric art with teal (rgb(20 184 166)) and amber (rgb(245 158 11)) color palette
- Animation delays via style attribute: `style="animation-delay: 1s"`

From NuxtUI (confirmed available components):
- UButton, UBadge, UIcon, UPageSection (all confirmed in @nuxt/ui 4.5.1)
- Icon names: i-lucide-check-circle, i-lucide-sprout, i-lucide-alert-circle, i-lucide-circle
- NOTE: UStepper does NOT exist in NuxtUI 4.x -- use custom ol with UIcon

Current entrypoint email HTML (line 176 of docker/entrypoint.sh):
```html
<h1>Welcome to LocalNodes!</h1>
<p>Your knowledge garden at <a href='SITE_URI'>SITE_URI</a> is ready.</p>
<p><a href='LOGIN_URL'>Click here to set your password and log in</a></p>
<p>This login link is single-use. If it expires, request a new one from the login page.</p>
<p>Happy gardening!<br>The LocalNodes Team</p>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build GardenAnimation, ProvisioningProgress, ProvisioningComplete components and rewrite success.vue</name>
  <files>
    localnodes-onboarding/app/components/GardenAnimation.vue,
    localnodes-onboarding/app/components/ProvisioningProgress.vue,
    localnodes-onboarding/app/components/ProvisioningComplete.vue,
    localnodes-onboarding/app/pages/success.vue
  </files>
  <action>
    Create three new Vue components and rewrite success.vue to use them with the useProvisioningStatus composable.

    **GardenAnimation.vue** -- Animated SVG garden visualization (see research Pattern 3):
    - 192x192 SVG viewBox with botanical scene: ground circle, stem, two leaves, center bloom, floating particles, growth rings
    - CSS animations via Tailwind keyframes: pulse for leaves/bloom, ping for particles/rings
    - Ambient glow behind SVG using radial-gradient with blur
    - Use v-motion only for the initial mount animation (`:initial` + `:enter` with spring transition)
    - Use the teal/amber color palette from HeroSection.vue (rgb(20 184 166), rgb(245 158 11))
    - Keep it self-contained (no props needed)

    **ProvisioningProgress.vue** -- Multi-step stage indicator (see research Pattern 4):
    - Props: `stages: Array<{ id: string; label: string; state: 'completed' | 'active' | 'pending' }>`
    - Render as `<ol>` with `<li>` for each stage
    - State icons: `i-lucide-check-circle` (completed, text-primary-500), spinning border div (active, border-primary-500), empty circle div (pending, border-(--ui-border))
    - Labels styled by state: completed = text-primary-400 line-through, active = text-white font-medium, pending = text-(--ui-text-muted)
    - v-motion on each `<li>` for staggered enter animation (delay: i * 100ms)
    - Do NOT use `<UStepper>` -- it does not exist in NuxtUI 4.x

    **ProvisioningComplete.vue** -- Success state (see research Pattern 5):
    - Props: `siteUrl: string`, `loginUrl: string | null`
    - Sprout icon (i-lucide-sprout, text-primary-500, size 64)
    - "Your garden is ready!" heading
    - Site URL displayed as clickable link
    - "Check your email for a login link" helper text
    - Two CTAs: "Visit Your Garden" (primary, links to siteUrl) and "Set Your Password" (outline, links to loginUrl, only if loginUrl provided)
    - v-motion on root element for fade-in + slide-up on mount

    **success.vue** -- Rewrite using composable (see research Pattern 6):
    - Import useProvisioningStatus with sessionId from `route.query.session_id`
    - Three conditional states:
      1. **Waiting** (not complete, not failed): GardenAnimation + "Growing your garden..." heading + timeRemaining countdown (font-mono, text-primary-400) + ProvisioningProgress with stages prop + helper text about welcome email
      2. **Complete** (isComplete): ProvisioningComplete with siteUrl and loginUrl props
      3. **Failed** (isFailed): Alert icon + "Something went wrong" + error message + "Back to Home" button (Phase 17 will expand)
      4. **No session** (!sessionId): "Back to Home" button only
    - Update useSeoMeta: title "Setting Up Your Garden - LocalNodes"
    - Wrap in UPageSection > max-w-md mx-auto text-center
  </action>
  <verify>
    <automated>cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npx vitest run</automated>
  </verify>
  <done>
    - GardenAnimation.vue renders an animated SVG garden with CSS keyframes and ambient glow
    - ProvisioningProgress.vue shows 4 stages with correct icons for completed/active/pending states
    - ProvisioningComplete.vue shows site URL and CTAs
    - success.vue switches between waiting/complete/failed/no-session states using composable data
    - All existing tests still pass (zero regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance welcome email with getting-started steps</name>
  <files>docker/entrypoint.sh</files>
  <action>
    Update the welcome email HTML in `docker/entrypoint.sh` (line ~176) to replace the basic message with a richer getting-started email. The email is sent via Resend API curl call inside the entrypoint's organizer provisioning block.

    Replace the existing `\"html\"` value with enhanced HTML containing:
    1. Heading: "Your knowledge garden is ready!"
    2. Intro paragraph with site URL link
    3. "Getting started" section as an ordered list:
       - "Set your password" -- click the button below (single-use, works once)
       - "Explore your garden" -- visit Groups, Events, and Topics to see the demo content
       - "Invite your community" -- use the Groups section to add members
       - "Try the AI assistant" -- ask the chat widget anything about your community's content
    4. Prominent CTA button: "Set Password and Log In" (teal background #14b8a6, white text, 12px 24px padding, 6px border-radius, inline-block)
    5. Expiry note: "This link is single-use and will expire. If it expires, visit [site URL]/user/password to request a new one."
    6. Sign-off: "Happy gardening, The LocalNodes Team"

    Important shell escaping notes:
    - The HTML is inside a JSON string inside a shell heredoc-style inline string
    - All double quotes inside the HTML must be escaped as `\"`
    - Use single quotes for HTML attribute values where possible (e.g., `style='...'`)
    - Keep HTML compact -- avoid unnecessary newlines inside the JSON string to prevent shell parsing issues
    - Use `&amp;` for ampersands in HTML entities
    - The `$SITE_URI` and `$LOGIN_URL` shell variables must remain unescaped (they are in double-quoted context)

    Test the shell escaping by checking that the JSON is syntactically valid (the curl command should not error on the payload).
  </action>
  <verify>
    <automated>cd /Users/proofoftom/Code/os-decoupled/fresh3 && bash -n docker/entrypoint.sh && echo "Shell syntax valid"</automated>
  </verify>
  <done>
    - Welcome email HTML contains "Getting started" ordered list with 4 steps
    - Email has a styled CTA button linking to the login URL
    - Email includes expiry/password-reset note
    - Shell syntax check passes (bash -n)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify progress UI and welcome email visually</name>
  <files>N/A</files>
  <action>Human visually verifies the progress UI renders correctly and the welcome email HTML looks correct.</action>
  <what-built>
    Complete provisioning progress UI: animated garden visualization, multi-step progress indicator with named stages, countdown timer, success state with Visit Your Garden CTA, and enhanced welcome email with getting-started steps.
  </what-built>
  <how-to-verify>
    1. Run `cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npx nuxi dev` and visit http://localhost:3000/success (no session_id) -- should show "Back to Home" button only
    2. Visit http://localhost:3000/success?session_id=test123 -- should show:
       - Animated garden SVG with pulsing leaves/particles
       - "Growing your garden..." heading
       - Countdown timer starting at ~4:00 in M:SS format
       - 4 named stages (first one should be active since test123 will not match real Redis data, so status stays unknown)
    3. Review the entrypoint email HTML: open `docker/entrypoint.sh` and verify the "Getting started" section has 4 numbered steps and a styled CTA button
    4. (Optional, if you want to trigger a real provisioning): observe that stages advance and the countdown ticks as the state machine progresses in Redis
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: human visual verification required"</automated>
  </verify>
  <done>User approves the progress UI appearance and welcome email content</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Full test suite passes:
```bash
cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npx vitest run
```

All components render without Vue warnings:
```bash
cd /Users/proofoftom/Code/os-decoupled/localnodes-onboarding && npx nuxi build 2>&1 | grep -i "error\|warn" | head -10
```

Shell syntax valid:
```bash
cd /Users/proofoftom/Code/os-decoupled/fresh3 && bash -n docker/entrypoint.sh
```
</verification>

<success_criteria>
- 4-stage progress indicator shows named stages with correct visual states (STAT-01)
- Animated garden SVG plays CSS animations during wait (STAT-02)
- Countdown timer ticks down from ~4:00 in M:SS format (STAT-03)
- Success page shows site URL and "Visit Your Garden" CTA when complete (STAT-04)
- Welcome email is sent by entrypoint (NOTIF-01 -- already working from Phase 15)
- Welcome email contains site URL, login link, and 4 getting-started steps (NOTIF-02)
- All existing tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/16-status-notification/16-02-SUMMARY.md`
</output>
